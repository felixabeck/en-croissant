# En Croissant has no project-scoped MCP servers.
# chrome-devtools is configured user-wide in ~/.grok/config.toml.
# This desktop app does not use Sentry or Supabase — do not add those servers here.
