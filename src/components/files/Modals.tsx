// File creation/editing lives in the native workspace flow. The old components used renderer
// filesystem paths and are intentionally kept as inert compatibility exports until callers are
// replaced by the workspace page controls.
export function CreateModal() {
  return null;
}
export function EditModal() {
  return null;
}
export function CreateDirectoryModal() {
  return null;
}
