# `build` run ledger

One line per `build` run in this repository. Everything else about a run can be reconstructed
afterwards from the transcripts (`~/.claude/scripts/agent-usage.py`) or from git. This file exists
for the one thing that cannot: **whether plan review caught defects earlier than the diff review
did.** If findings migrate from the "final" column to the "plan" column over time, the plan-review
stage is paying for itself; if they do not, it is ceremony.

Duration per phase is a measurement, not a limit. It has no ceiling on purpose — a time limit would
be a second split axis beside area cohesion and would invite splitting a coherent area because it
looks slow, against universal rule 4a.

| Date | Task | Phases | Model / effort per phase | Duration per phase | Fix rounds | Plan findings | Final findings | Gates | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-30 | `remove-tree-unhardened` cluster: f-20260830-02/-03/-04/-05, the recursive workspace delete | 2 | 1: `sol`/`medium` (sensitive path `src-tauri/src/infra/**`) · 2: `luna`/`xhigh` | 1: ~9 min · 2: ~8 min | 2 (one per phase, both after the diff review) | **2 rounds, 7 lenses each: 7 REVISE, then 6 REVISE + 1 APPROVED.** Round 2's findings were largely new rather than repeats. 4 reversed a design decision, 2 exposed tests that pinned nothing, 3 became filed findings. | 6 lenses, 6 REVISE: 3 blockers on one defect (the delete stayed fallible after it had destroyed data), 2 blockers on tests proving the seam instead of the shipped code, 1 on a wrong category name. All fixed. | fmt, check, clippy, test (317), coverage:backend + ratchet, lint:ci, both boundary checks, bindings:check, coverage:report:test, bundle:report:test, test:coverage (266), coverage:frontend:check, build-vite, bundle:check, findings check, container e2e 8/8 — all green | Plan review earned its keep twice over: `st_dev` alone was wrong for same-filesystem bind mounts, and the `openat2` option named in the findings would not have closed the primary one. Both were caught before any code existed. The diff review then caught what the plan could not: the *tests* looked thorough and pinned nothing — the mount test forced the detector's answer, so deleting the whole detector left it green. Three revert experiments were run by hand rather than trusting the leaf's claims, and one of them disproved a claim. |
| 2026-08-30 | `gate-scripts` cluster: f-20260829-04/-09/-14/-15, the coverage ratchet, the in-place mutation runner and the shared findings tool | 5 (4 code, 1 ledger) | 1: `luna`/`xhigh` · 2: `sol`/`medium` (sensitive: `package.json`, `.github/workflows/**`) · 3-5: orchestrator inline · fix round: `sol`/`medium` | 1: ~8 min · 2: ~17 min · 3-5: ~12 min inline · fix round: ~6 min | 1 fix round after the diff review, plus two orchestrator-inline corrections (one after phase 1, two after the fix round) | **3 rounds, 6 lenses each: 6 REVISE, then 5 REVISE + 1 APPROVED, then 6 REVISE.** Round 1 reversed the park's justification; round 2 found six design defects including that `spawnSync` gives the cargo child no owner and that a blanket `git checkout` would destroy concurrent work; round 3 found that no phase actually wrote the ledger. Three lenses independently confirmed the park is a genuine external constraint. | 5 lenses, 5 REVISE: 1 real defect (a fence stranded when setup fails before any spawn), 5 test gaps, 4 naming/duplication, 2 restatements of the recorded aggregate-ambiguity hold. | mutation:guard:check, findings.py check, findings:test 3/3, mutation:runner:test 18/18, coverage:report:test 16/16, bundle:report:test, both boundary checks, lint:ci, test:coverage 59 files, coverage:frontend:check, build-vite, bundle:check, cargo fmt/check/clippy, cargo test 317, bindings:check, test:coverage:backend, coverage:backend:check, container e2e 8/8 — all green | **The revert experiments earned their keep twice, and both times against a claim, not a guess.** The implementing leaf named the test that would fail if its cleanup were reverted; the test passed anyway, because there were two cleanup sites and only one was covered. Re-checking then exposed that the test named `a fence read failure before spawn` never created a fence at all — `umask 777` made the directory unreadable, so the run died before the exclusive create and the assertion "no fence remains" was trivially true against a runner with no cleanup whatsoever. Neither of the five diff lenses caught either one. Also worth recording: the plan review reversed my own park justification in round 1 and I re-tested the constraint instead of arguing — which is the only reason the park now rests on a measurement. |
| 2026-08-30 | `f-20260830-44`, fork identity: bundle identifier, updater severance, GPL-3 §5(a) notice | 3 code + 6 review-fix leaves | 1: `sol`/`medium` (sensitive: `tauri.conf.json`, `oauth.rs`) · 2: `sol`/`medium` (sensitive: capabilities, `main.rs`, `package.json`) · 2b/2c: `luna`/`xhigh` + `sol`/`medium` (coverage recovery) · 3: `luna`/`xhigh` · fixes A,B,D,E: `luna`/`xhigh` · C,F: `sol`/`medium` | 1: ~4 min · 2: ~11 min (stopped red) · 2b: ~13 min · 2c: ~7 min · 3: ~14 min · fix fan-out: ~9 min · fix F: ~6 min | 2 inside phase 2 (both coverage ratchets went red as a *consequence* of the change, not a mistake in it), plus 2 orchestrator-inline corrections | **2 rounds, 7 then 4 lenses: 4 REVISE, then 2 REVISE.** Round 1 reversed the central design — `review-root-cause` (98) refuted substituting a new OAuth client-id literal, because the root cause is that the identity exists twice and a substitution leaves the drift mechanism intact. It also caught that the notice named no modifier (2 lenses, 100), a `; test $? -eq 1` that turned any failing gate into success (3 lenses, 99-100), and a proof that never read `tauri.conf.json` and so could pass with the pubkey intact. Round 2 caught a self-contradiction I introduced between "set `createUpdaterArtifacts: false`" and a proof asserting its absence, and that the OAuth test proved propagation but not sourcing. `review-code-quality` found `CONTRIBUTING.md` — five occurrences of the old identifier that the locate stage never searched. | 6 lenses, 4 REVISE + 2 APPROVED: 1 real leak (three lenses independently), 1 over-broad `existsSync`, 1 misleading function name, 1 silent OAuth error path, 2 doc statements falsified by the change, 3 test gaps. All fixed. 3 deferred and filed: f-20260830-46, -47, -48. | mutation:guard, fmt, check, clippy, cargo test 327, coverage:backend + ratchet, bindings, lint:ci, both boundary checks, i18n:check + jsx, coverage:report:test, bundle:report:test, ui-boundary tests, test:coverage, coverage:frontend:check, build-vite, bundle:check, `pnpm build`, container e2e 8/8, findings check, findings:test — 24/24 green | **Two lessons, both about instruments rather than code.** First: I filed a finding (`f-20260830-45`) declaring the backend coverage gate down, from a crash I reproduced on inputs the gate never uses — a concurrent session in the same checkout measured it properly and refuted three of my claims. I asked Felix a question built on that false premise. Reproducing a failure is not the same as reproducing *the* failure. Second: both coverage ratchets went red as a direct consequence of correct work — deleting a well-tested module, and adding tests whose assertions count against branch coverage (`f-20260829-04`). Both times the cheap fix was available and wrong; both times the honest fix was to cover the area's genuinely untested code, which is now 8 tests on `operation.ts` and 10 on real refusal paths in `oauth.rs`/`credentials.rs`. Also: two sessions ran in this checkout at once, which the `build` lock does not prevent because the other was not a `build`. |
| 2026-08-30 | Adopt agent-workflow tooling from ChessRiddle and Korrigio: canonical skill tree, gate routing, workflow hygiene, toolchain parity, harness enforcement, findings parity, security lens, coordination docs, gate receipts | 9 code + 5 review-fix leaves | 1-6,9: `sol`/`medium` (sensitive: `package.json`, `.github/workflows/**`) · 7-8: `luna`/`xhigh` · fixes 1-4: `sol`/`medium` · fix 5: `luna`/`xhigh` | 1: 8.6 · 2: 9.6 · 3: 5.3 · 4: 4.4 · 5: 3.3 · 6: 4.6 · 7: 8.4 · 8: 9.9 · 9: 6.5 min · fix fan-out ~7.8 min wall | 3 phases stopped RED and were repaired before commit (5, 7, 9); 5 orchestrator-inline fixes | **2 lenses (`review-plan` `sol`/high, `review-minimalism` `luna`/xhigh), both REVISE: 10 blockers + 2 should-fix.** Caught three consumers of the old canonical path including a `.grok` rule file never opened; the `release.yml` mutable-tag supply-chain exposure; that coverage does not measure `scripts/**`, so the planned coverage gates were pure cost and `node --test` suites are the only proof; and an entire missing section (toolchain parity). Also: the routing checker would have rejected its own `cargo` commands. | 5 lenses, all REVISE: 6+6+6+5+5 findings. Three blockers reproduced by hand before fixing — a 6-space-indented workflow granting `contents: write` with `evil/unpinned@main` passed with OK; the env hook allowed a `cp` of a secret when its parser was unavailable; the receipt's pre/post fingerprint missed a mutate-then-revert. All blockers fixed with regression tests confirmed red against the old code; 2 findings filed (`f-20260830-54`, `-55`). | mutation:guard, lint:ci, both boundary checks, coverage:report:test, bundle:report:test, test:coverage (62 files / 275 tests), coverage:frontend:check, build-vite, bundle:check, bindings:check, findings.py check, findings:test, plus 15 tooling checks — all green | **Plan review earned its keep on evidence I could not have produced myself: it found a supply-chain exposure in the one `contents: write` job, and it falsified a factual claim I had written into the plan about what coverage measures. The diff review then earned it again — every one of the four new enforcement checkers had a bypass, and three were demonstrated rather than argued. The lesson worth keeping: a checker written and verified by the same run is not evidence it enforces anything; only an adversarial attempt to walk past it is. Also two self-inflicted incidents: an unescaped backtick in an `echo` executed `git push` and put four unreviewed commits on the fork (universal rule 18b, in a shape the rule does not name), and `mutation:runner:test` was left red by phase 1 for eight commits because I never ran it per-commit — CI would have caught it, I did not.** |
| 2026-08-31 | `native-fs` cluster: f-20260829-11 and f-20260830-06/-07/-22/-26/-27/-39/-40 — error payloads, the atomic fault seams, archive extraction, the dead path authority, the engine read capability, subtree record pruning, zip opening books, the manifest trust boundary, and the gate that anchors two of them | 10 (A, 1-7, Z, L) + 2 review-fix leaves | A,1,2,3,4,5,6: `sol`/`medium` (sensitive: `src-tauri/src/fs.rs`, `infra/**`, `db/**`, `game.rs`, `engines/**`) · 7,Z: `luna`/`xhigh` · L: orchestrator inline · fixes 1-2: `luna`/`xhigh` (forced — see note) | A: ~13 · 1: ~13 · 2: ~15 · 3: ~11 · 4: ~14 · 5: ~19 · 6: ~7 · 7: ~9 · Z: ~13 min · fix fan-out ~14 min wall | 0 phase fix rounds; 4 orchestrator-inline corrections (a rustfmt nit phase 6 correctly refused to touch, an oxlint `valid-expect` rejection, two artifact durability stages that had been collapsed, two stale doc line-ranges) | **4 rounds — 11, 11, 8, 5 lenses: 7 REVISE, 8 REVISE, 8 REVISE, then 3 REVISE + 2 APPROVED.** Round 1 refuted the fault-seam design (an injection-free inner cannot hold sixteen in-operation points) and found that an unrestricted operation backfill would mint a durable engine root on every restart. Round 2 found the plan naming a renderer error category that does not exist, and a umask-`0o022` mode test that stays green on a revert. Round 3 showed per-site redaction leaves the mechanism — eight producers, `Serialize` sending the whole `Display` — which produced phase A, and caught that "Phase 0 first, committed last" is incoherent. Round 4 found `CommitDurability` separately Specta-exported, `get_file_metadata` still leaking a raw `symlink_metadata` failure, a component test with no file, and that no phase owned `tasks/**`. | 10 lenses, 8 REVISE + 2 APPROVED. 11 findings against this run's own work, all fixed: two tests that pinned nothing (the component test mocked SWR so the fetcher never ran; the removal tests never reopened the registry), logging that no test observed, and a five-copy duplication this cluster's own first commit introduced. 14 pre-existing defects filed. | mutation:guard, fmt, check --all-targets, clippy -D warnings, backend-test (362), backend-coverage + ratchet, lint:ci, both boundary checks, rust:surface:check, bindings:check, coverage:report:test, bundle:report:test, gates:receipt:test, hooks:check, frontend-coverage + ratchet, frontend-build, bundle:check, workflows:check, workflows:permissions:test, tools:parity:check, findings check + test + parity, container e2e 8/8, tauri-build (release) — all green | **Plan review paid for itself four times, and the pattern is that it kept refuting the *shape* of a fix rather than its details.** Twice it reversed a design before code existed: the fault seam, where the file already contained the correct pattern twenty lines from the one being replaced, and the error redaction, where fixing three named sites would have left five. Once it caught the plan contradicting a recorded decision — `d-20260830-05` names the renderer category `partially-applied` while the shipped code implements `applied-despite-error` and explains in a comment why. Three lenses refused to guess between them, correctly; `d-20260831-01` corrects the record beside the original rather than rewriting it. **The recurring lesson is about anchors: four lenses across two rounds refused to accept "annotate the existing gate-scripts finding" as a regression proof for a deletion and a cfg-gating, and they were right — annotating a finding is not a test. That objection is the only reason `scripts/check-rust-release-surface.mjs` exists, and it was verified by hand to fail on both reverts before its commit.** Two findings' own suggested fixes were wrong and are rejected with evidence in their closing notes. Also recorded: `gpt-5.6-sol` returned "model is at capacity" on three review leaves, so those ran on `luna`/`xhigh` — a provider constraint, not a ladder choice. |
| 2026-08-31 | `gate-scripts` cluster, `findings.py`-sharing slice: f-20260830-14 and f-20260830-15 — the pending `_atomic_write` port to the sibling copies, and the missing divergence detection between them | 0 code leaves (implementation-only downgrade after plan review: one file, no sensitive-path glob) + 0 fix leaves; 8 review leaves across 3 rounds | implementation: orchestrator inline · `review-plan`: `luna`/`max` (forced — `sol` returned "model is at capacity" twice) · all other lenses `luna`/`xhigh` | plan review r1 ~14 min · r2 ~6 min · implementation ~12 min · diff review ~9 min · close-out ~8 min | 0 phase fix rounds; 1 full orchestrator fix round against the diff review (12 findings) | **2 rounds — 5 then 3 lenses: 5 REVISE, then 3 REVISE.** Round 1 reversed the central design and falsified two of the plan's factual claims. `review-code-quality` (99) and `review-plan` (99) showed the asserted star topology was false — `chess-tactics-app` does carry a parity test, pinning `correction-app`. `review-code-quality` (97) then surfaced `d-20260826-10` and Felix's 2026-08-27 chat qualification, which had already tried this run's proposed hard staleness gate and measured eight commits stranded unpushed; D-a flipped from blocking to advisory. Four lenses independently caught that the promised `_run_main` fixture would recurse, since `main` runs the whole suite. `review-error-handling` (91) caught that `REF..HEAD` cannot see a diverged pin. Round 2 found no design defect: `list --open` cannot prove a `handled` status (3 lenses, 99), and the probe had no defined failure outcome against `d-20260830-20`. | 5 lenses, 3 REVISE + 2 APPROVED, 12 findings — all fixed, 1 deferred to two other repositories. Two were real logic defects, not coverage gaps: the `PIN-AHEAD` fallback labelled an ancestor commit `NEWER` and claimed it unreachable from the pin (both false), and the WARN heading contradicted the evidence printed under it. Six test gaps, each then confirmed by mutating the production line and watching the named test go red. `review-tests` (99) caught the integration test deriving its expected remedies from the very tuple under test. | mutation:guard, findings.py check, findings:test, findings:parity:check, gates:routing:check — all green. No frontend/backend gates: the diff touches only `scripts/findings-parity-tests.py` and `tasks/**`. | **The finding was substantially stale, and reading the repository beat reading the entry.** f-20260830-15 asked for a divergence gate that already existed, already failed in both directions, and was already wired into `$push`; f-20260830-14 asked for a port to two siblings, one of which had landed it independently the day before. Roughly two thirds of what the entries described was already done, and the run's value was finding the third that was not: nothing observed the upstream moving past a pinned commit. **The sharpest lesson is that a lens found a decision I did not know existed and would have violated.** My first plan proposed exactly the hard staleness gate ChessRiddle had already tried, measured, and had Felix overrule — and it survived a full round of my own reasoning before `review-code-quality` produced the citation. Precedent search across sibling repositories is not optional when the artefact is shared across them. Also: `d-20260830-21`'s shape was measured to fail — its uncommitted repair of `correction-app/scripts/findings.py` was gone and the Python 2 `except` breakage was back, committed, with that repository's entire findings CLI dead since `4fc4803ac`. Repaired again instrumentally and filed there, but the real lesson is recorded as `d-20260831-13`: a working-tree-only repair is scaffolding, not a fix. |
| 2026-08-31 | `frontend-ui` cluster, sliced to f-20260829-02 alone: the 320px/200% font-scale layout, and the e2e assertion that was meant to catch it | 0 (parked before the plan — the authority boundary gates the whole deliverable, not one step of it) | 3 locate probes `luna`/`xhigh`, read-only; 2 native Playwright geometry runs driven by the orchestrator | locate fan-out ~7 min wall · measurement runs ~1 min each · close-out ~10 min | n/a | n/a — no plan was written, so no plan review ran | n/a | mutation:guard not run (no `src-tauri` touch); findings.py check, findings:test 3/3, findings:parity:check 20/20 — green. The diff is `tasks/**` only | **The run stopped at the point where a plan would have been written, and the reason is worth recording precisely: this finding cannot be closed by any diff that does not also refresh four committed 320px snapshots, and every spelling of the container refresh route is denied in `.claude/settings.json`. The deny was probed once, with a project name matching no test so nothing could be rewritten, and it fired. Retrying under another phrasing is the circumvention `CLAUDE.md` names when it says "a determined invocation can phrase a command differently" — so the run parked instead. Three shippable sub-deliverables were considered and each collapses to the same wall: the new assertion goes red on today's tree by construction, the layout fix moves the pixels, and an assertion added but not called is dead code. **What the run did buy is that the follow-up is implementation-only.** The defect was measured rather than described — 83 clipped elements on `/settings`, 27 after opening Appearance, 2 on `/accounts`, against `documentElement.scrollWidth` reporting 320 while content sits at x=353 — and four root causes were pinned to file and line, including one the finding never mentioned: the title bar spends 3 x 2.8125rem = 270px of a 320px bar on window controls, which is where the snapshot's stray "cht" fragment comes from. Two design questions that would otherwise have cost the next session a plan-review round each are settled as `d-20260831-16` and `d-20260831-17`. **The measurement method is the transferable part:** classifying overflow as lost-versus-reachable by walking for a scrollable ancestor before a clipping one is what turns "the layout looks wrong" into a number a gate can hold, and it was run natively on the strength of `d-20260829-01` — geometry is machine-independent, only rasterisation is not.** |
