# Plan-review handoff: f-20260905-11 installer provenance

Plan (gitignored): `tasks/plans/2026-09-19-install-local-provenance.md`
Drain session: `2b746a66-8148-4ad1-8f23-b1e420cf6bb7`
Executor: Claude (`DRAIN_EXECUTOR=claude`). Orchestrator: Grok 4.6. Implementation-only downgrade after freeze: the orchestrator wrote phase 1; Claude lenses reviewed the cumulative diff.

## Mandate

Close f-20260905-11. `scripts/install-local.sh` must not record provenance `reviewed` for a binary it never bound to HEAD. Holes: `--no-build` copies `target/release` as reviewed; dirty check uses `--untracked-files=no`; no recheck after `pnpm build`. The filed `@{upstream}` vs remote `upstream` remark is out of mandate: `@{upstream}` is the fork tracking ref `$push` reviews.

## Rounds

`plan_adopted_per_round` r1=10 r2=4 r3=3 r4=3 r5=0

Raw lens artefacts: `/tmp/build-2b746a66-8148-4ad1-8f23-b1e420cf6bb7/lens-*.txt` (plan r1–r5 and `*-diff`).

All adopted issues and Skip/withdrawals are in the plan `## Reviews` (R1-01..R5-03). Frozen at r5: every required lens APPROVED; leftover should-fix on merge-base exit 128 skipped (fail-closed as not-ancestor).

## Implementation

- `6707cb04` bind reviewed provenance to this invocation's build
- `ec6347ea` share upstream refusal and pin post-build missing upstream

Proof: `pnpm install:local:test` 27/27.

Successor: load this file before reviewing those commits. No open successor finding.
