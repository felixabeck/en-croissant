# Plan-review record: f-20260914-15 command-entry refusals

Load this file before reviewing a successor of this slice. Frozen plan:
`tasks/plans/2026-09-18-windows-command-refusal-ordering.md` (gitignored). Snapshots:
`/tmp/build-01a0b4e1-f-20260914-15/plan-r1.md` … `plan-r3.md`.

**Mandate:** Close `f-20260914-15`. Remaining unported production commands refuse with
`Error::Conflict` from `platform_support` before in-process work. Completeness is the
`replace_pgn_atomic` call graph, not the 117-command Specta registry.

**Slice:** `f-20260914-15` only. Left open at `build`: `f-20260914-29`, `f-20260917-04`,
`f-20260917-13`. `f-20260916-01` stays the closed-world of refusal *occurrences*.

**Decision:** `d-20260918-13`.

## Issues

| ID | Origin | Disposition | Closure |
| --- | --- | --- | --- |
| R1-01 | review-plan | Fix | Split cargo TESTNAME (measured exit 1). Closed r2. |
| R1-02 | review-plan | Fix | Named `edit_existing` (`pgn.rs:690`). Closed r2. |
| R1-03 | review-tests, review-root-cause, review-error-handling | Fix | Production-region scan + transitive Specta pin. Closed r2 for stated gaps. |
| R2-01 | review-plan, review-tests, review-error-handling | Fix | Split `mod tests` whose cfg mentions `test` (`db/mod.rs` is `all(test, unix)`). Closed r3. |
| R2-02 | review-error-handling, review-tests | Fix | Crate-wide production-caller allowlist of every path node, including `pub` cores. Closed r3. |
| R2-03 | review-plan | Fix | Risk bullet. Closed r3. |
| R3-01 | review-tests | Skip | Token match vs rustc aliases; named limitation; no analyzer. |
| D-min | review-minimalism (diff) | Fix | `for_each_production_region` shared by both pins. `bfc8f2b0`. |
| D-count | review-tests (diff) | Fix | Production `off_unix_refusal(` count equals `guard_rows().len()`. `bfc8f2b0`. |

`plan_adopted_per_round`: r1=3 r2=2 r3=0. Three plan-review rounds, all `VERDICT: APPROVED`.
Nine round-1 lenses; four r2; four r3. Nine cumulative-diff lenses, all `APPROVED`.

Raw reports: `/tmp/build-01a0b4e1-f-20260914-15/lens-*.txt`, `lens-r2-*.txt`, `lens-r3-*.txt`,
`lens-diff-*.txt`.

## Code

- `0ab696a9` — first-statement guards on `delete_game`, `write_game`, `export_to_pgn`; pins; `LIVE_REFUSAL_ROW_COUNTS = (0, 6)`.
- `bfc8f2b0` — shared scan helper and `off_unix_refusal` count pin.
- `4538e0db` — `d-20260918-13`.

Executor: DeepSeek Flash via Claude CLI (`routed=claude`). Orchestrator: Grok. Family
separation: the same DeepSeek family wrote the code and ran the lenses; disclosed.
The orchestrator (Grok) arbitrated triage.
