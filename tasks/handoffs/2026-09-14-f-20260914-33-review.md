# f-20260914-33 — plan-review and diff-review record (canonical acquisition of symlinked-ancestor pathnames)

Durable copy of the complete review history for `f-20260914-33` ("a file or folder selected through a
symlinked directory gets a grant that can never be promoted or used"), preserved under universal
rule 12a because the plan (`tasks/plans/2026-09-14-dialog-grant-symlinked-ancestor.md`), its round
snapshots and the raw lens reports under the sessions' scratch directories are ignored or ephemeral.

* **Finding:** `f-20260914-33` (closed by this run).
* **Final plan revision:** r7, approved after 7 plan-review rounds; a pre-implementation re-trace on
  2026-09-15 found no semantic change (no revision, no delta round).
* **Implementation:** one phase, Codex write leaf at the sensitive rung; commits listed under
  "Commits" below.
* **Successors:** `f-20260914-36` owns R1-06 (load-time rebinding of entries persisted before this
  fix) and the annotated R3-05 and R3-06 (identity-only use-time validations in
  `database_file_target` and `workspace_root`). `f-20260915-03` owns D1-01 (resolution proves the
  leaf identity but not the parent identity). Each successor loads this record before its own plan
  review.
* **Sessions:** plan review in session `3e814b4d-ae05-49a4-88ad-331f803947b1` (2026-09-14/15);
  implementation, diff review and closure in session `a680a996-f64c-4cff-b72c-b092cfeac59a`
  (2026-09-15).
* **Metrics:** 7 plan-review rounds, 65 unique plan issues, `plan_adopted_per_round` r1=9 r2=14
  r3=10 r4=9 r5=5 r6=4 r7=0, active plan review ≈ 165 min (2026-09-14 22:05 → 2026-09-15 00:50
  CEST, no pauses); diff-review rounds and their issue counts under "Diff review".

MANDATE (fixed across all rounds): the `Defect`, `Why it matters` and `Open question` bullets of
`f-20260914-33` in `tasks/findings.md`; Felix, 2026-09-14: "Do the next finding from the ledger.
Full auto, Executor Codex."

## Plan review

### Round 1 — r1, seven Codex lenses (review-plan at review-plan rung; others sensitive)

Raw verdicts: review-plan REVISE · review-minimalism REVISE · review-tauri-security REVISE ·
review-correctness REVISE · review-root-cause REVISE · review-tests REVISE ·
review-error-handling REVISE.

| ID | Claim (witnesses, confidence) | Disposition | Correction |
|---|---|---|---|
| R1-01 | PGN creation door creates by pathname before acquisition; cleanup unlinks by pathname (plan 91, tauri-security 97, error-handling 96) | Fix | r2 C; r3 C adds parent identity, grant_acquired, cfg split |
| R1-02 | PGN test ambiguous (plan 95, tests 96) | Fix | Test 2 |
| R1-03 | Test 3 used `workspace_mutation_target`, which creates nothing (plan 94) | Fix | Tests 1, 3 |
| R1-04 | Root door acquires twice via migration fallback (minimalism 99, root-cause 93, error-handling 90) | Fix | r3 B: one `(spelling, identity)`, fallback `persist_entry` |
| R1-05 | Duplicates `database_file_target`'s sequence (minimalism 90) | Fix | A |
| R1-06 | Load-time rebinding outside MANDATE (5 lenses, 89-99) | Defer | `f-20260914-36` |
| R1-07 | Refusing leafless selections outside MANDATE (5 lenses, 87-99) | Fix | A2; r3 test 7 adds `real/..` |
| R1-08 | Keep `reject_disagreeing_expected_identity` (correctness 95) | Fix | B |
| R1-09 | Engine-image cleanup lexical parent compare vs canonical spelling (correctness 97) | r1 Skip → **withdrawn r2**, see R2-07 | r3 B rule |
| R1-10 | Test 7 replaced the leaf, not an ancestor (root-cause 97, tests 98) | Fix | Test 8 |
| R1-11 | Test 4 asserted only ids (tests 94) | Fix | Test 4 |
| R1-12 | Rebinding tests incomplete (tests 97, 93) | Moot (R1-06) | — |
| R1-13 | Rebinding swallows errors (error-handling 99) | Moot (R1-06) | in `f-20260914-36` |

### Round 2 — r2, same seven lenses and rungs

Raw verdicts: all seven REVISE. Closure reported: R1-02, -03, -05, -08, -11 CLOSED by all
reporting lenses; R1-01 NOT CLOSED (tauri-security, correctness, tests); R1-04 NOT CLOSED (tests);
R1-07 NOT CLOSED (tests, error-handling: `real/..` untested); R1-10 NOT CLOSED (root-cause:
promotion window; tauri-security: pre-canonicalisation window).

| ID | Claim (witnesses, confidence) | Disposition | Correction (r3) |
|---|---|---|---|
| R2-01 | `promote_dialog` still ancestor-following; swap between grant and promotion persists an unusable spelling (correctness 99, root-cause 99, tauri-security 96, error-handling 95) | Fix | B promotion re-proves; test 9 |
| R2-02 | PGN door re-acquires through grant after creation; existing destination identity unrecorded (plan 94, minimalism 96, correctness 98, root-cause 98) | Fix | C6 `grant_acquired` with the creation identity |
| R2-03 | Swap before `canonical_binding` is followed; plan claimed refusal for the whole window (tauri-security 94, root-cause 89); for PGN creation it can create in an attacker directory (tauri-security 97) | Fix | Goal restates the guarantee; decision 6; C2/C4 parent identity; test 10(d) |
| R2-04 | PGN body uses Unix-only symbols behind a runtime refusal (plan 99, correctness 99) | Fix | C two-block counterpart |
| R2-05 | Cleanup check and unlink separate; reuse existing helper (plan 92, minimalism 91, tauri-security 95) | Fix (reuse `remove_entry_at`) | C7; residual two-syscall window noted as POSIX limit |
| R2-06 | `sync_all` failure before `created` recorded leaks the file (plan 90, error-handling 99) | Fix | C5 |
| R2-07 | R1-09 skip unsound: `AuthorizedDir` swap makes `register_engine_image` store a canonical spelling that `cleanup_engine_images` drops (correctness 97) | Fix | B rule: descriptor-verified doors unchanged; test 5 |
| R2-08 | Persistent-file and root registration beyond MANDATE (minimalism 92, tests 94) | Skip with evidence | The folder pickers `issue_database_workspace_blocking`, `issue_engine_workspace_blocking`, `issue_puzzle_workspace_blocking` and `register_opening_book` pass user-selected pathnames through these doors — the finding's "user who picks a file or folder through a symlinked directory" |
| R2-09 | Proof command passes three filters to one `cargo test` (plan 99, root-cause 97, tests 99) | Fix | Phase proof split into five commands |
| R2-10 | Test 9 named the wrong hook test (plan 99) | Fix | Test 11 |
| R2-11 | No PGN creation swap test (tests 98) | Fix | Test 10(c)(d) |
| R2-12 | Leafless `real/..` untested (tests 94, error-handling) | Fix | Test 7 |
| R2-13 | Fresh persistent-file path never exercised (tests 91) | Fix | Test 4 opening book fresh |
| R2-14 | Existing PGN destination reuse lacks a content sentinel (tests 92) | Fix | Test 10(b) |
| R2-15 | No swap-window test for root/persistent acquisition (tests 96) | Fix | Test 8 root case |
| R2-16 | Ancestor-swap refusals surface as `Error::Io` → renderer "unexpected" (error-handling 93) | Skip | Every existing no-follow refusal (`workspace_mutation_target`, atomic replacement, `database_file_target`) returns that variant; one door diverging would make the taxonomy inconsistent; not required by MANDATE |
| R2-17 | Discarded PGN cleanup error (error-handling 99) | Skip (settled by `f-20260914-24`) | Named in C7 and Not part |

Cumulative after r2: unique issues 30; adopted r1=9, r2=14; withdrawn 1 (R1-09 skip); deferred 1;
skipped 3; moot 2. Active review ≈ 50 min.

### Round 3 — r3, same seven lenses and rungs

Raw verdicts: review-plan REVISE · review-minimalism APPROVED · review-tauri-security APPROVED ·
review-correctness REVISE · review-root-cause REVISE · review-tests REVISE ·
review-error-handling APPROVED. Closure: all adopted R1/R2 corrections CLOSED by minimalism,
tauri-security, correctness and error-handling; NOT CLOSED — R2-03 (plan: pre-canonicalisation PGN
race untested; root-cause: root activation window), R1-01/R1-04/R2-01 identity arm/R2-02/R2-05/R2-06
(tests: no test reaches their failure branches).

| ID | Claim (witnesses, confidence) | Disposition | Correction (r4) |
|---|---|---|---|
| R3-01 | PGN hook after canonicalisation leaves the identity-capture-to-canonicalisation window untested (plan 97) | Fix | C3 hook before canonicalisation; test 10(c)(d) |
| R3-02 | No test reaches an error after PGN creation (plan 96, tests 97) | Fix | Tests 10(e)(f) with the existing atomic-file injector |
| R3-03 | Acquisition before shape checks changes error precedence (plan 97) | Fix | B: shape checks first; `check_dialog_grant_shape` |
| R3-04 | Same-inode relocation claimed and untested; outside MANDATE (plan 94, tests 98) | Fix (wording) | Goal and decision 6: no guarantee either way |
| R3-05 | `database_file_target` compares identity only; a swapped ancestor mints a carrier at `moved/x.db3` (correctness 98) | Skip → annotated into `f-20260914-36` (`7cb518cd`) | Same inode, today's `d-20260912-04/05` behaviour; enforcing path equality at use would refuse legacy symlinked-spelling database entries that work today, which is that finding's open question |
| R3-06 | Picker roots activate through ancestor-following `workspace_root`; swap before activation commits an unusable active root (root-cause 97) | Skip → annotated into `f-20260914-36`; Goal wording narrowed to descriptor-relative steps | Activation is a use-time validation of an already persisted entry, the same class as R3-05 |
| R3-07 | Test 5 persisted a `link/...` spelling without modelling the `AuthorizedDir` swap (root-cause 94) | Fix | Test 5 rewritten around `authorize_existing_dir` + swap |
| R3-08 | Test 10(b) never checks the existing-file branch's stored spelling or use (tests 97) | Fix | Test 10(b) |
| R3-09 | Test 9 lacks a different-inode case (tests 96) | Fix | Test 9(b) |
| R3-10 | No test proves the root door acquires once (tests 95) | Fix | `ACQUIRE_TARGET_CALLS`; tests 5, 12 |
| R3-11 | Red-today evidence has no command or artefact (tests nit 91) | Fix | Tests preamble: per-test command and log |
| R3-12 | New PGN leaf's parent directory not fsynced before the registry names it (error-handling 92) | Fix | C5 parent `fsync` |
| R3-13 | `create_regular_at` can fail after `O_CREAT` before an identity exists, leaking the file (plan 88, error-handling 91) | Skip | An unlink without identity is pathname trust; shared with `create_database_child`; C7 records it |

Cumulative after r3: unique issues 43; adopted r1=9 r2=14 r3=10; withdrawn 1; deferred 1 (+2
annotated into it); skipped 6; moot 2. Active review ≈ 75 min.

### Round 4 — r4, five lenses (review-plan, correctness, root-cause, tests, error-handling; minimalism and tauri-security APPROVED r3, not re-run)

Two leaves (root-cause, tests) died at start with Codex "Selected model is at capacity" and were
relaunched once with the same prompts. Raw verdicts: review-plan REVISE · review-correctness
APPROVED · review-root-cause APPROVED · review-tests REVISE · review-error-handling APPROVED.
Closure: correctness closed all its items; root-cause NOT CLOSED R2-06, R2-07/R3-07; plan NOT
CLOSED R2-05, R2-06, R3-11; tests NOT CLOSED R1-01, R1-04, R2-02, R2-05, R2-06, R3-03, R3-11,
R3-12; error-handling closed all.

| ID | Claim (witnesses, confidence) | Disposition | Correction (r5) |
|---|---|---|---|
| R4-01 | `open_parent_no_follow` does not exist (plan 99) | Skip | C4 defines it as the exposure of `unix::open_parent`; Phases lists the `fs.rs` change |
| R4-02 | File/parent sync failure branch untested; injectors cover only the registry write (plan 96, root-cause 93, tests 96) | Skip | C5 makes the guarded region start at `create_regular_at`'s return with one exit through C7; test 10(e) drives that exit. A sync-failure seam would be a new test harness (rule 6d) for ordering that the phase diff review and the cumulative lenses read directly |
| R4-03 | Hook moved without citing `d-20260912-05` (plan 93) | Fix | A4 cites it; validate-before-binding preserved |
| R4-04 | A failed PGN promotion leaves the inserted dialog grant (plan 94, error-handling 97) | Fix | C7 removes it; test 10(e) asserts the grant count (superseded at implementation by D1-08: `promote_dialog` consumes the grant on every failure) |
| R4-05 | Red-today log path and test names undefined (plan 98, tests 91) | Fix | Absolute log path and named tests in the preamble |
| R4-06 | Trailing-separator destination accepted after `canonical_binding` (correctness 91) | Fix | C1; test 10(g) |
| R4-07 | Descriptor-verified persistent-file path (`register_engine_image`) unguarded (root-cause 94, tests 92) | Fix | Test 5 second case |
| R4-08 | No mutation of the created PGN before grant (tests 99) | Fix | `PGN_EXPORT_POST_CREATE_HOOK`; test 10(h) |
| R4-09 | Cleanup not proven descriptor-relative (tests 98) | Fix | Test 10(e) link retarget |
| R4-10 | Shape-precedence untested (tests 95) | Fix | Test 13 |
| R4-11 | Race matrix omits the opening-book door and different-inode swaps (tests 94) | Skip | One shared `acquire_target` proof walk; test 8 drives it through three different callers, test 9(b) and the existing post-validate hook test cover a different inode |
| R4-12 | Counter cannot detect a second raw `validate_target` (tests 92) | Skip | R1-04's hazard is re-canonicalisation between proof and persistence; a raw re-validation of the proven canonical spelling cannot change the stored pair, and the counter pins the canonicalising call |
| R4-13 | Test 9(a) not marked red-today (error-handling 93) | Fix | Marked |

Cumulative after r4: unique issues 56; adopted r1=9 r2=14 r3=10 r4=9; withdrawn 1; deferred 1 (+2
annotated); skipped 10; moot 2. Active review ≈ 100 min.

### Round 5 — r5, five lenses (review-plan, correctness, root-cause, tests, error-handling)

Raw verdicts: review-plan REVISE · review-correctness APPROVED · review-root-cause REVISE ·
review-tests REVISE · review-error-handling APPROVED. Closure: every adopted issue CLOSED by
correctness and error-handling; plan and root-cause closed all but R3-11/R4-05; tests closed all but
R4-04 (existing-file branch) and R3-11/R4-05. R4-02 accepted as a recorded limitation by plan and
tests.

| ID | Claim (witnesses, confidence) | Disposition | Correction (r6) |
|---|---|---|---|
| R5-01 | Test 8 listed as red-today but needs the new hook and counter (plan, root-cause 99) | Fix | Removed from the red-today list |
| R5-02 | Red-today test names not concrete (plan, tests 96) | Fix | Six exact test function names in the preamble |
| R5-03 | Test 10(f) lacks the grant-count assertion (tests 96) | Fix | Test 10(f) |
| R5-04 | No test proves the grant itself stores the canonical spelling (tests 93) | Fix | Test 2 asserts the grant's stored spelling before promotion |
| R5-05 | Reuse tested only link → real (tests 91) | Fix | Test 4 reverse direction for puzzle root and persistent file |

Cumulative after r5: unique issues 61; adopted r1=9 r2=14 r3=10 r4=9 r5=5; withdrawn 1; deferred 1
(+2 annotated); skipped 10; moot 2. Active review ≈ 125 min.

### Round 6 — r6, closure lenses (review-plan, root-cause, tests)

Raw verdicts: review-plan REVISE · review-root-cause APPROVED · review-tests APPROVED. Closure:
R5-01..R5-05, R3-11/R4-05 and R4-04 CLOSED by all three.

| ID | Claim (witnesses, confidence) | Disposition | Correction (r7) |
|---|---|---|---|
| R6-01 | PGN door builds `AcquiredTarget` with `parent_and_leaf: None` although it holds the proving descriptor, contradicting A's invariant (plan 98) | Fix | Invariant comment names the caller-retained case |
| R6-02 | Test 10(e) retargets `link` after canonicalisation, so a pathname cleanup of the canonical spelling still passes (plan 98) | Fix | Hook replaces `dir/real` with a new directory holding a sentinel; asserts the sentinel survives and the created file (now under `moved`) is gone |
| R6-03 | Promotion shape precedence with an unavailable target untested (tests 97) | Fix | Test 13 promotion cases |
| R6-04 | PGN door's existing symlink / non-regular leaf untested (tests 96) | Fix | Test 10(i) |

### Round 7 — r7, closure lenses (review-plan, tests)

Raw verdicts: review-plan APPROVED · review-tests APPROVED. Closure: R6-01..R6-04 CLOSED by both.
No new issue.

### Plan convergence

Latest raw verdict per lens: review-minimalism APPROVED (r3) · review-tauri-security APPROVED (r3)
· review-correctness APPROVED (r5) · review-error-handling APPROVED (r5) · review-root-cause
APPROVED (r6) · review-plan APPROVED (r7) · review-tests APPROVED (r7). Every adopted substantive
correction had a reviewer closure result against the revision carrying it. Deferred: R1-06 →
`f-20260914-36` (with R3-05, R3-06 annotated into it). Recorded skips: R2-08, R2-16, R2-17, R3-05,
R3-06, R3-13, R4-01, R4-02, R4-11, R4-12. Withdrawn: R1-09's skip. Totals: 7 rounds, 65 unique
issues, `plan_adopted_per_round` r1=9 r2=14 r3=10 r4=9 r5=5 r6=4 r7=0.

### Pre-implementation re-trace (2026-09-15, HEAD `b8d271e0` = origin/master)

35 commits (macOS engine launch, `f-20260914-31`/`-32`) landed after the plan base `30574719`. A
hunk-by-hunk diff of `infra/fs.rs`, `infra/path_authority/mod.rs`, `main.rs`, `puzzle.rs` and
`file_workspace.rs` found no semantic change to any symbol the plan names (`canonical_binding`,
`open_verified_parent`, `unix::open_parent`, `create_regular_at`, `remove_entry_at`,
`create_pgn_export_destination`, `grant_dialog_operations`, `promote_dialog`,
`migrate_legacy_os_path_inner`, `get_or_create_persistent_file_inner`, `get_or_create_root`,
`database_file_target`, the test hooks); the changes were additive. No revision, so no delta round.

## Implementation

Phase 1 ran as one Codex write leaf (`--role sensitive`). Red-today evidence: the six named tests
failed on unmodified production code (NotADirectory at a real write; stored `link/...` instead of
`real/...` for grant, root, opening book and migration; promotion succeeding after an ancestor swap;
the PGN export door refusing a symlinked parent). After the production change the leaf reported
proof 3 red on `promotion_refuses_a_different_leaf_inode_after_grant`; the orchestrator reproduced
it (red 5/5 in the module run, green alone): the test removed the file before recreating it, so the
filesystem handed back the freed inode number and identity (dev, ino) matched. The replacement is
now created before it is renamed over the original. All five proof commands then green
(path_authority 220, file_workspace 35, fs 80). The advisory cross clippy for
`aarch64-apple-darwin` and `x86_64-pc-windows-gnu` could not run: the C cross toolchains the
`-sys` build scripts need are not installed on atlas (the plan made it conditional on them); the
macOS and Windows CI jobs remain the evidence for the cfg split.

## Diff review

### Round 1 — `b8d271e0..2b640767`, seven Codex lenses at the sensitive rung

The first `review-tauri-security` leaf died at start ("Selected model is at capacity"); it was
relaunched once with the same prompt. Raw verdicts: review-correctness APPROVED ·
review-root-cause APPROVED · review-code-quality APPROVED · review-tests APPROVED ·
review-error-handling APPROVED · review-minimalism REVISE · review-tauri-security REVISE.

| ID | Claim (lens, confidence) | Disposition | Reason / correction |
|---|---|---|---|
| D1-01 | Resolution proves the stored leaf identity but not the parent identity; a replaced parent holding a hard link to the leaf passes, and sibling writes (sidecar, WAL, temporaries) land in the replacement (`mod.rs` `retained_workspace_target` and peers; tauri-security 98, rated blocker) | Defer → `f-20260915-03` | Pre-existing use-time boundary; closing it needs a persisted parent identity (registry schema change, unrecoverable for existing entries) — an open design question, same class as `f-20260914-36` |
| D1-02 | Recursive deletion checks identity, then unlinks by name (`fs.rs` `remove_tree_at` regular-file and directory arms; tauri-security 99) | Skip (duplicate) | Open as `f-20260830-09` ("every removal unlinks by name; Linux offers no way to unlink by descriptor"); the code comments name the residual window |
| D1-03 | Sidecar-failure rollback in `create_workspace_file_blocking` unlinks the new PGN by name, deleting a concurrently substituted file (`file_workspace.rs` ~797; tauri-security 97) | Fix | `50db4e10`: `remove_entry_at` with the installed identity; `remove_regular_at` deleted; two tests (rollback removes its PGN; a replaced PGN survives with `OperationAndCleanup`) |
| D1-04 | `AcquiredTarget` doc claims Unix mutations use the retained descriptor although registration doors drop it (code-quality 98) | Fix | `ffddee3e` doc |
| D1-05 | `get_or_create_persistent_file` doc limited to bundled files although the opening-book picker uses it (code-quality 97) | Fix | `ffddee3e` doc |
| D1-06 | One PGN export test bundles plan cases 10(c)-(i) (code-quality 94) | Fix | `ffddee3e`: one test per case, shared setup |
| D1-07 | Hook test still named after the removed post-validate hook (code-quality nit 91) | Fix | `ffddee3e`: `database_file_target_rejects_a_leaf_replaced_before_the_proof` |
| D1-08 | A failed `promote_dialog` leaves the one-use grant stored; callers drop the id, so it lingers until TTL and can evict live grants (error-handling 86) | Fix | `ffddee3e`: grant consumed at lookup; all production callers grant and promote in one call (orchestrator trace); four tests that pinned preservation (from `4494070f`, no recorded decision) now assert consumption; new conflict test. Supersedes the per-door grant removal of plan issue R4-04. Decision recorded in `tasks/decisions.md` |
| D1-09 | `persist_entry` duplicates the fresh `StoredEntry` construction (minimalism 93) | Fix | `ffddee3e`: `fresh_stored_entry` |
| D1-10 | The expected-identity acquire/validate branch is copied into three doors (minimalism 91) | Fix | `ffddee3e`: `registration_target` |
| D1-11 | The database symlinked-ancestor test registers through canonicalising migration, so `database_file_target` never meets the symlink (tests 98) | Fix | `ffddee3e`: raw `link/x.db3` spelling via the descriptor-verified door; the leaf showed it red against a stored-spelling revert |
| D1-12 | `2b640767` left two `platform_support` source scans red (`refusal_guards_are_first_statements…`: the PGN door is no longer a first-statement guard; and the refusal-label table), because phase proofs did not include that module (orchestrator, full `cargo test`) | Fix | `ffddee3e`: PGN door listed as a typed non-unix counterpart and label row; `50db4e10` drops the `remove_regular_at` rows. Full suite 1189 passed |

Round-1 counts: 12 unique issues (11 lens, 1 orchestrator gate); adopted 10 (Fix), deferred 1,
skipped 1. Decisions recorded: `d-20260915-03` (canonical acquisition design) and `d-20260915-04`
(a failed promotion consumes its dialog grant).

### Round 2 — cumulative `b8d271e0..ffddee3e`, six Codex lenses at the sensitive rung (the lenses that drove round-1 fixes, plus correctness for the grant-consumption behaviour change)

Raw verdicts: review-code-quality APPROVED · review-minimalism APPROVED · review-tests APPROVED ·
review-error-handling APPROVED · review-tauri-security APPROVED · review-correctness APPROVED.
Closure: D1-04, D1-05, D1-06, D1-07 CLOSED (code-quality); D1-09, D1-10 CLOSED (minimalism);
D1-11 CLOSED (tests); D1-08 CLOSED (error-handling, confidence 95); D1-03 CLOSED (tauri-security),
D1-01 confirmed deferred to `f-20260915-03`, D1-02 confirmed duplicate of `f-20260830-09`;
correctness found no defect in the grant-consumption change. D1-12 (orchestrator gate) closed by the
green full backend suite.

| ID | Claim (lens, confidence) | Disposition | Correction |
|---|---|---|---|
| D2-01 | `AcquiredTarget` doc claims a canonical pair although leafless and non-unix acquisitions keep the caller's spelling (code-quality 97) | Fix | doc qualifier |
| D2-02 | `get_or_create_persistent_file` doc claims canonicalisation off Unix (code-quality 95) | Fix | doc qualifier |
| D2-03 | `normal_leaf` Option computed and discarded (code-quality nit 91) | Fix | boolean predicate |
| D2-04 | Persistent-class → `AcquireShape` mapping duplicated in `promote_dialog` and `registration_target` (minimalism nit 91) | Fix | one mapping helper |
| D2-05 | The two symlinked-workspace tests in `file_workspace.rs` duplicate their setup (minimalism nit 94) | Fix | shared test fixture |

Round-2 counts: 5 unique issues, adopted 5; no deferral or skip. Correction commit `86ab3738`.

### Round 3 — cumulative `b8d271e0..86ab3738`, the two lenses that drove round-2 fixes

Raw verdicts: review-code-quality APPROVED · review-minimalism APPROVED. Closure: D2-01, D2-02,
D2-03 CLOSED (code-quality); D2-04, D2-05 CLOSED (minimalism).

| ID | Claim (source, confidence) | Disposition | Correction |
|---|---|---|---|
| D3-01 | `symlinked_ancestor_workspace_state` repeats the authority/grant/promotion wiring of `workspace_state` (minimalism nit 86) | Fix | `5cd3b559`: both route through `promoted_workspace_state` |
| D3-02 | `x86_64-pc-windows-gnu` clippy `--all-targets -D warnings` fails: `AcquiredTarget::parent_and_leaf` is never read off Unix (orchestrator cross clippy with check-only CC/AR/windres stubs; introduced in `2b640767`) | Fix | `78287a7c`: field `#[cfg(unix)]`. No lens re-run: compile-only change, Unix semantics identical; proven by native, `aarch64-apple-darwin` and `x86_64-pc-windows-gnu` clippy and the full suite (1189 passed) |

The first cross clippy attempt (without stubs) failed in C build scripts of `-sys` crates; the stub
recipe from `tasks/handoffs/2026-09-14-macos-engine-launch-review.md` made the check runnable.

### Round 4 — closure check on `5cd3b559` (review-minimalism)

Raw verdict: review-minimalism APPROVED. D3-01 CLOSED (confidence 98). No new issue.

### Diff-review convergence

Latest raw verdict per lens: review-correctness APPROVED (round 2) · review-root-cause APPROVED
(round 1) · review-tests APPROVED (round 2) · review-error-handling APPROVED (round 2) ·
review-tauri-security APPROVED (round 2) · review-code-quality APPROVED (round 3) ·
review-minimalism APPROVED (round 4). Every adopted correction has a reviewer closure result, except
the compile-only D3-02 and the gate-only D1-12, whose closure evidence is the green gate named in
their rows. Open issues: none.

Totals: 4 diff-review rounds; 19 unique issues (D1: 12, D2: 5, D3: 2); adopted 17, deferred 1
(D1-01 → `f-20260915-03`), skipped 1 (D1-02, duplicate of `f-20260830-09`). Two issues were found by
orchestrator gates rather than lenses (D1-12, D3-02); both were defects of the phase-1 commit whose
phase proofs did not include `platform_support` tests or the Windows cross clippy.

## Commits

* `2b640767` fix(path-authority): bind user-selected pathnames to their proven canonical parent (phase 1)
* `bd0b725a` docs(findings): file f-20260915-03 (D1-01)
* `50db4e10` fix(workspace): roll back only the PGN this creation installed (D1-03)
* `ffddee3e` fix(path-authority): close diff review round 1 (D1-04..D1-12)
* `9e342f6f`, `f3553c86` docs(decisions): d-20260915-03, d-20260915-04
* `86ab3738` refactor(path-authority): close diff review round 2 (D2-01..D2-05)
* `78287a7c` fix(path-authority): keep AcquiredTarget's proving descriptor Unix-only (D3-02)
* `5cd3b559` test(workspace): share the grant-and-promote fixture for PGN workspaces (D3-01)
