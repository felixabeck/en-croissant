# Plan-review handoff: f-20260914-11 Windows startup registries

Load this record before reviewing a successor of this slice. It carries the issue IDs, raw
verdicts, dispositions, and the two withdrawn mechanisms (degraded-mode startup; pulling
`f-20260905-10` / `f-20260917-13` into the same slice).

## Mandate

`f-20260914-11`: on Windows, startup could not persist the path-authority and credential
registries or create app-owned roots. Slice: this finding only. Left open at `entry=build`:
`f-20260914-12`, `f-20260914-13`, `f-20260914-15`, `f-20260914-29`, `f-20260917-04`,
`f-20260917-13`. `f-20260905-10` stays open (`create_dir_all`).

## Executor / family

Orchestrator Grok. Executor DeepSeek V4.1 Flash via the Claude Code CLI (`routed=claude`)
for locate, plan review, and both write phases. Family separation for cumulative review is
the same family as the writer unless a later push lens used a different executor; disclose
that.

## Plan-review rounds

`plan_adopted_per_round`: r1=8 r2=5 r3=6 r4=0 (r4 APPROVED; typographical acceptance-line
and Phase-2 marker-pin split after r4 did not change mechanism).

| Round | Raw verdicts | Adopted |
|---|---|---|
| r1 | plan/correctness/root-cause/tests REVISE; others APPROVED | R1-01..09 |
| r2 | correctness/root-cause/tests/code-quality REVISE; plan APPROVED | R2-01..06 |
| r3 | tests/minimalism REVISE; plan/correctness/root-cause APPROVED | R3-01..07 |
| r4 | all four re-reviewed lenses APPROVED | — |

Issue IDs R1-01..R1-09, R2-01..R2-06, R3-01..R3-07 are in
`tasks/plans/2026-09-18-windows-startup-registries.md` `## Reviews` (gitignored working
plan). Summary of adopted mechanisms that a successor must not re-derive:

- Port `remove_leaf_identified` via existing `remove_entry_at` so the 18th ignore can go.
- Pin updates in the same phase as the production deletions.
- Reparse/junction leaves are `Error::Io` with inner `"reparse point"` *before* `identity()`.
- Fixture is `cmd /C mklink /J`, extracted as `mklink_junction` in `windows_tests`.
- One shared verbatim `APP_OWNED_DEFAULT_ROOT_LEAVES` table.
- `relative_components` is cfg-gated; the `open_regular_relative` body is ungated and must
  not use `Path::components()`. Discriminating test: target file exists, `"a//b"` still Err.
- Phase E ungated pin includes `ensure_app_owned_default_dir`. Marker pin: zero
  `f-20260914-11` attributes in credentials/oauth/main, via shared `unported_marker`.
- Do not pull `f-20260905-10` or `f-20260917-13` (`d-20260918-03`, `d-20260918-05`).

## Code

- `6abf3956` feat(path-authority): port app-owned dirs and AuthorizedDir doors on Windows
- `1a1d39ed` test(windows): un-ignore startup registry tests and pin the new doors
- Decisions `d-20260918-04` .. `d-20260918-07`

## Successor load

A follow-up that touches these doors must load this file before plan review. Inherited
open IDs: none from this slice. Related open findings under the same root are listed in
the Mandate section and are not inherited as unfinished review issues.
