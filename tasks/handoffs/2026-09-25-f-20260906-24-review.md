# Review history: f-20260906-24

Plan author and final arbiter: Codex, in one interactive context. Plan detection ran on Gemini through the selected executor. Code implementation ran through a Codex leaf. All seven cumulative diff lenses first attempted Gemini, hit its quota, and retried once in fresh Codex contexts; detection therefore ran on the same model family as the code. The raw verdicts below remain the reviewers' claims, separate from arbitration.

## Plan review arbitration

Round 1 opened P1–P9. All were adopted as Fix: P1 listing admission added scope, P2 cross-workspace memory contamination, P3 unbounded legacy bytes, P4 malformed and legacy owner trust, P5 compression compatibility, P6 single oversized ID behavior, P7 concrete bounds, P8 proof commands and integration coverage, P9 unnecessary generic UI changes. Round 2 closed P1–P9 and opened P10–P13: absent-workspace migration, oversize before eviction, compression proof, and old owner-test updates. All four were adopted as Fix. Round 3 closed P10–P13. No plan issue remains open.

The implemented repair after cumulative review changed the plan's legacy migration choice. A legacy array has no workspace provenance; its IDs are discarded after the original owner snapshot rather than tagged with the currently active workspace. This is recorded in d-20260925-01, superseding d-20260924-06. Repair of valid over-budget records moved to startup even when Files is never opened; an oversized UI-only ID no longer blocks later valid saves. This is recorded in d-20260925-02, superseding d-20260924-07.

## Cumulative diff review arbitration

The seven read-only lenses inspected the implementation commit against e7050d8d1b15f40b114ab6fbf055df3641c32624. All first attempted Gemini and exhausted quota; all retried once with Codex. Every actionable finding was adopted as Fix. The persisted-state and root-cause lenses independently found the unscoped legacy attribution defect. Correctness found inactive-workspace oversized records left physically over budget. Error handling found that a transient oversized ID blocked later valid saves. Test review found gaps for malformed or throwing storage and workspace identity. Code quality and minimalism found constant, duplication, and helper issues. One additional source trace by the arbiter found that Files need not mount at startup, so quota repair cannot wait for atom hydration. No reviewed finding was deferred.

The repair passed 35 focused renderer tests, `pnpm exec tsgo --noEmit`, `pnpm lint:ci`, and `git diff --check`. The release binary built. `pnpm verify:app` reached the independent stale-file scenario and timed out waiting for its reload/conflict panel; this separate verifier observation was added to open f-20260924-06. The harness cannot exercise expansion through a native workspace picker, so the direct Jotai and storage tests are the behavioral proof for this change. The container screenshot suite and full frontend gates run under the push workflow.

## Raw plan review findings and verdicts


### lens-review-correctness-r1.txt

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — In-memory expansion state is not cleared or scoped when switching workspaces (`FilesPage.tsx:105`, `DirectoryTree.tsx:156`). Input: user expands directory `dirA` in workspace `ws1` (persisting `{ version: 1, workspaceId: "ws1", expanded: ["dirA"] }`), switches collection via `chooseWorkspace()` to `ws2`, and expands `dirB` in `DirectoryTree`. Result: `setExpanded((old) => [...old, handleKey])` receives `old` containing `dirA` from `ws1` and appends `dirB`, persisting `{ version: 1, workspaceId: "ws2", expanded: ["dirA", "dirB"] }`. This leaks `ws1` capabilities into `ws2` and causes `pathOwners.ts` to retain `dirA` for `ws2` on subsequent startup, violating plan line 9 ("A change of workspace makes the prior expansion record inactive") and line 24 ("Rejected: carry IDs across workspaces, because IDs are native capabilities from a different authority context"). (confidence: 95)

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Logical contradiction between enforcing a hard serialized-byte budget and "including a long single ID". Input: an expanded directory ID whose individual serialized length exceeds the serialized-byte budget (e.g. a single ID of 8,192 bytes with a budget of 4,096 bytes). Result: if the budget is enforced as a hard ceiling, 0 IDs can fit, so keeping the single ID violates the byte budget and risks exhausting shared `sessionStorage` quota contrary to MANDATE `f-20260906-24`. If the budget is bypassed to keep the single ID, the byte limit invariant fails. The plan must explicitly define the failure contract: an oversized single ID cannot be persisted to storage and must report through `reportPreferenceStorageFailure` as an unpersisted session interaction, rather than claiming to keep it within the budget. (confidence: 92)

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — Contract and timing mismatch between "only IDs observed in the current listing may be admitted" and atom hydration before listing availability (`atoms.ts:132`, `DirectoryTree.tsx:68`). Input: app startup or page reload where `expandedDirectoriesAtom` hydrates from `sessionStorage` during module evaluation while `tauri.listFileWorkspace` in `FilesPage` has not yet resolved. Result: if listing admission is enforced during atom hydration (or before returning hydrated state, line 12), all stored IDs are dropped because no listing is yet observed, destroying expansion state on every reload. Conversely, if admission is not reconciled when the directory tree listing arrives, stale IDs for deleted or moved directories remain in `expandedDirectoriesAtom` indefinitely, continuing to be persisted and retained across startups (`f-20260924-01`). The plan must separate atom hydration (restoring bounded stored IDs for the workspace) from listing reconciliation in `DirectoryTree` once `files` resolves. (confidence: 90)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:15 — Snapshot reader risks treating an inactive or workspace-mismatched record as malformed/untrusted (`pathOwners.ts:156`). Input: `sessionStorage["expanded-directories"]` contains a valid record for `ws1`, but `localStorage["file-workspace"]` currently holds `ws2` (or is null). Result: if the snapshot reader requires `workspaceId === currentWorkspace` as part of schema validation, validation fails and marks `trustedFamilies` without `expandedDirectories`. In native startup reconciliation (`src-tauri/src/infra/path_authority/mod.rs:2171-2177`), an untrusted `ExpandedDirectories` family aborts unowned capability cleanup for all `PgnWorkspace`, `PgnFile`, and `PgnReadOnlyFile` entries. The plan must clarify that an inactive record for a different workspace retains 0 IDs but remains trusted (`trusted: true`); only schema corruption or invalid JSON leaves the family untrusted. (confidence: 88)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — Eager repair writes on read in preference storage conflict with the non-rewriting invariants (`src/state/utils.ts:61`). Input: `sessionStorage` contains a legacy `string[]` array. Result: existing `createZodStorage` executes `write(key, parsedValue, "repair")` on `getItem()` whenever raw input does not equal parsed output. This triggers an unhandled write during initial read/startup. The plan must specify that the shared expansion storage module decouples legacy parsing and bounds enforcement from storage writes, keeping `getItem()` and pre-hydration queries strictly read-only and normalizing only on subsequent user writes. (confidence: 86)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Unspecified numeric bounds for the fixed count and serialized-byte budget across atom and snapshot (`tasks/plans/2026-09-24-expanded-directory-storage.md:28`). Input: `pathOwners.ts` and `atoms.ts` implementing retention bounds independently. Result: line 28 states that the startup snapshot and the atom must agree on record shape and limits, but lines 11–16 provide no concrete constants (e.g. count <= 1,000 IDs per `f-20260924-01` and a concrete byte budget such as 32 KB or 64 KB). Without explicit constants in the contract, independent implementations risk divergent truncation thresholds. (confidence: 85)

VERDICT: REVISE


### lens-review-error-handling-r1.txt

[blocker] /home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Directing hydration failure through the existing preference-error path ([`createZodStorage`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/utils.ts#L64-L68)) swallows parse exceptions into a silent default and performs a destructive repair write that replaces malformed storage with `[]`. While line 15 requires that "Malformed state leaves the family untrusted instead of silently discarding ownership" and that the snapshot does not rewrite storage, [`createZodStorage`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/utils.ts#L64-L68) catches parse failures, logs [`warn`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/platform/native.ts), and executes `write(key, initialValue, "repair")`. When this repair write succeeds, [`reportPreferenceStorageFailure`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/utils.ts#L19-L28) is never called, the parse error is returned as success (`[]`), and the corrupted record keeping native capability reconciliation fail-closed is erased from storage. On the subsequent reload, [`collectOriginalSnapshots`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/pathOwners.ts#L156-L163) reads a valid empty record, trusts `expandedDirectories` with 0 retained IDs, and native [`reconcile_startup_owners`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src-tauri/src/infra/path_authority/mod.rs#L6253-L6327) permanently sweeps directory capabilities. Hydration must preserve raw bytes without executing a repair write on invalid/malformed records (matching [`engineOwnerStorage.test.ts:204`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/engineOwnerStorage.test.ts#L204)), and report read failure truthfully. (confidence: 96)

[blocker] /home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Omitting the failure outcome when a single directory ID exceeds the serialized-byte budget allows over-budget expansions to be dropped silently as a successful save. For multi-item collections, LRU recency eviction drops older IDs to keep the latest expansion within budget (line 21). However, when a single newly expanded directory ID alone exceeds the serialized-byte budget, it cannot fit in storage. If the bounding layer drops it before persisting and writes an empty list `{ version: 1, workspaceId, ids: [] }`, storage succeeds without error, claiming a successful save while omitting the expanded directory from persistence. MANDATE `f-20260906-24` explicitly requires "truthful storage failure handling"; an expansion that cannot be admitted or persisted due to exceeding the byte budget must trigger a save failure reporting through [`reportPreferenceStorageFailure("save", ...)`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/utils.ts#L19-L28) rather than returning success with dropped data. (confidence: 93)

[should-fix] /home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md:9,24 — Unspecified invalidation failure path when existing storage holds a legacy `string[]` across a workspace change. Line 9 allows legacy `string[]` to remain valid for the current workspace until normalized on the next successful write, while line 24 rejects carrying IDs across workspaces. Because legacy `string[]` records lack a `workspaceId` tag, if a workspace switch occurs and the storage invalidation or clearing write fails (or if no expansion occurs in the new workspace to trigger a write), the legacy record remains in `sessionStorage`. On subsequent reload, [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/pathOwners.ts#L156-L163) and [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/atoms.ts#L132-L136) attribute the stale IDs to the new workspace, violating the workspace lifetime isolation contract. The plan must specify that switching workspaces actively purges or resets legacy storage and handles purge failures through [`reportPreferenceStorageFailure`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/utils.ts#L19-L28). (confidence: 88)

[should-fix] /home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md:12 — The plan fails to specify numerical bounds for the "fixed count" and "serialized-byte budget", violating `.claude/rules/async-resource-invariants.md:59` ("Bound anything that accumulates... and say what the bound is") and `.claude/rules/persisted-state.md:21-24`. Without stated bounds, verification cannot test count and byte threshold handling against the shared ~5 MB session storage origin quota. (confidence: 89)

[nit] /home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md:15 — The pre-hydration snapshot reader in [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r1/src/state/pathOwners.ts#L91-L98) does not specify its failure direction when `file-workspace` is unreadable or malformed in `localStorage`. Comparing `expanded-directories` against the current workspace requires a readable current workspace identity; if `file-workspace` fails to read or is malformed, `expandedDirectories` must also fail closed as untrusted rather than matching against a null/empty workspace. (confidence: 84)

VERDICT: REVISE


### lens-review-minimalism-r1.txt

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — Acceptance criterion "only IDs observed in the current listing may be admitted" is an unmandated scope expansion. Mandate `f-20260906-24` requires bounding disposable expansion storage and scoping retention to the active workspace, not synchronizing against a live directory listing. Deleting this breaks nothing because [DirectoryTree.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/DirectoryTree.tsx) already restricts user toggles to entries in the rendered tree, whereas keeping this criterion artificially pulls [FilesPage.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/FilesPage.tsx) into scope and introduces fragile coupling to asynchronous SWR listing fetches (confidence: 95)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:34 — Scope bloat in Phase 1: [FilesPage.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/FilesPage.tsx) and [DirectoryTree.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/DirectoryTree.tsx) should be cut from the plan. [FilesPage.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/FilesPage.tsx) does not touch `expandedDirectoriesAtom`, and [DirectoryTree.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/DirectoryTree.tsx) already operates over a `string[]` contract (`expanded.includes(...)`, `setExpanded(old => ...)`). By keeping the atom's external contract as `string[]`, [DirectoryTree.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/components/files/DirectoryTree.tsx) requires zero changes. The minimum-diff counterfactual touches only 2 existing files ([atoms.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/atoms.ts), [pathOwners.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/pathOwners.ts)) and 1 new domain module ([expandedDirectories.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/expandedDirectories.ts)), cutting UI modifications entirely (confidence: 92)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:34 — Proposed "shared bounded-storage module" risks introducing a generic abstraction without a second caller. Existing state modules use either [`createZodStorage`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/utils.ts#L30) or dedicated domain storage helpers ([layoutStorage.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/layoutStorage.ts), [engineOwnerStorage.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/engineOwnerStorage.ts)). The shared unit should be a concrete, domain-specific module (`src/state/expandedDirectories.ts`) sharing only the record schema, bounds constants, and parsing/truncation helper between its two actual callers: [atoms.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/atoms.ts) and [pathOwners.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r1/src/state/pathOwners.ts) (confidence: 88)

[nit] tasks/plans/2026-09-24-expanded-directory-storage.md:18 — "pixels use the pinned container" adds unnecessary verification requirements; bounding session-storage expansion state changes no visual styles, layout, or markup, so container pixel snapshot verification is not a valid acceptance criterion for this headless persistence change (confidence: 85)

VERDICT: REVISE


### lens-review-persisted-state-r1.txt

[blocker] src/components/files/DirectoryTree.tsx:156 — Stale in-memory expansion IDs leak across workspaces on context switch. The plan specifies that a workspace switch makes the prior expansion record inactive and rejects carrying IDs across workspaces, but omits how `expandedDirectoriesAtom` is reactively bound to `fileWorkspaceAtom`. In `DirectoryTree.tsx:156-181`, expanding a directory executes functional updates (`setExpanded((old) => [...old, handleKey])`). If the user explores Workspace A, switches to Workspace B via `FilesPage.tsx`, and expands any folder in Workspace B, `old` still holds the in-memory array of directory capability IDs from Workspace A. The subsequent save persists both Workspace A's and Workspace B's IDs under Workspace B's handle, leaking foreign capability IDs into the new workspace record and causing `pathOwners.ts` to retain invalid capabilities at startup. (confidence: 95)

[blocker] src/state/atoms.ts:132 — Disagreement on physical storage encoding between the atom writer and startup pre-hydration reader. Currently `atoms.ts:135` writes raw JSON via `createZodStorage` while `.claude/rules/persisted-state.md` mandates `serializeStorageValue` / `deserializeStorageValue` compression for structures sharing the ~5 MB `sessionStorage` quota with tab trees. If the planned shared bounded-storage module writes compressed UTF-16, `pathOwners.ts:159` (`readJson`) throws a `SyntaxError` on `JSON.parse` during the startup snapshot, causing `collectOne` to return `false` and marking the entire `expandedDirectories` family untrusted on every launch after a write. Both sides must explicitly agree on `serializeStorageValue` and `decodeCompressedOrJson` (which transparently handles both compressed UTF-16 and legacy JSON fallback). (confidence: 93)

[blocker] src/state/pathOwners.ts:56 — Pre-hydration snapshot falsely marks valid legacy arrays and bounded records untrusted. `collectOne` strictly requires `equal(read.value, parsed.data)`. When `sessionStorage` contains a legacy `string[]` or a record requiring count/byte boundary truncation, the parsed normalized record differs from `read.value`, causing `equal(read.value, parsed.data)` to evaluate to `false`. `collectOne` then returns `false` and marks `expandedDirectories` untrusted, skipping startup capability reconciliation on existing user installations that hold valid legacy arrays or grandfathered expansion state. The pre-hydration reader must parse and bound through the shared module without routing normalization differences through `collectOne`'s identity check. (confidence: 91)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — Lifecycle ordering conflict between hydration and listing-based admission. The plan requires that "only IDs observed in the current listing may be admitted" and to "Apply bounds before writing and before returning hydrated state". At application startup and atom hydration, `listFileWorkspace` (`FilesPage.tsx:88`) has not yet executed because it is an asynchronous IPC call triggered by `useSWR` when the view mounts. If hydration filters against the active listing, all persisted expansion state is purged on every app launch or page reload. The plan must clarify that initial hydration validates against workspace ID and count/byte budgets, while listing-based admission applies during user tree interaction or post-listing reconciliation. (confidence: 88)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Missing explicit numeric limits for the fixed count and serialized-byte budget. `.claude/rules/persisted-state.md` and `async-resource-invariants.md` require specifying exact bounds for data sharing the ~5 MB `sessionStorage` quota with tab trees and feeding native startup authority reconciliation (`MAX_AUTHORITY_IDS` = 4096). The plan specifies that bounds exist ("fixed count and serialized-byte budget"), but leaves the values undefined. Concrete caps (e.g., maximum 1,000 IDs and maximum 64 KB serialized budget) must be specified to prove that worst-case expansion cannot starve tab trees or trigger native `ResourceLimit` errors at startup. (confidence: 89)

VERDICT: REVISE


### lens-review-plan-r1.txt

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:34 — Phase 1 targets `src/components/files/FilesPage.tsx`, but the proof command (`pnpm exec vitest run src/components/files/DirectoryTree.test.tsx src/state/pathOwners.test.ts src/state/expandedDirectories.test.ts`) omits `src/components/files/FilesPage.test.tsx`. `FilesPage.test.tsx` specifies strict hoisted mocks of `@/state/atoms` and `jotai.useAtom` that only mock `fileWorkspaceAtom` and `fileWorkspaceDisplayNameAtom` (`src/components/files/FilesPage.test.tsx:23-48`). If `FilesPage.tsx` is modified to wire expansion reset or listing admission, `FilesPage.test.tsx` will fail, yet the proof command would stay green.
(confidence: 92)

[blocker] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — plan specifies that "only IDs observed in the current listing may be admitted" as part of workspace-scoped retention and hydration. In the actual application, `tauri.listFileWorkspace` is fetched asynchronously by SWR only after `FilesPage` mounts (`src/components/files/FilesPage.tsx:86-90`); no listing exists in memory or storage during startup. Pre-hydration path-owner collection (`src/state/pathOwners.ts:66-192`) and atom hydration run synchronously before IPC results exist. Attempting to enforce listing-observed admission at hydration drops all valid expanded directories on launch. Furthermore, this restriction is an addition beyond MANDATE `f-20260906-24` (`tasks/findings.md:7560-7564`), which requires disposable retention bounds, workspace scoping, legacy compatibility, and failure handling, but not UI-listing admission.
(confidence: 95)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — plan specifies that legacy `string[]` reads "remain valid for the current workspace and are normalized on the next successful write" and that snapshot reads must not rewrite storage (lines 9, 15). If the implementation delegates storage parsing to `createZodStorage`, `createZodStorage.getItem` automatically executes a repair write to storage whenever parsed content differs from raw input (`src/state/utils.ts:60-63`) or fails schema parsing (`src/state/utils.ts:64-68`), prematurely mutating storage during read instead of deferring normalization to the next write.
(confidence: 90)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — plan specifies that "any failed read, repair or save reports through the existing preference-error path". `reportPreferenceStorageFailure` is a private, unexported function in `src/state/utils.ts:19-28`. The planned new module (`src/state/expandedDirectories.ts`) cannot use this path without either exporting `reportPreferenceStorageFailure` or duplicating its error localization logic.
(confidence: 88)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:34 — proof command specifies `pnpm exec tsc --noEmit`. The project does not define a `tsc` script or list `typescript` in `devDependencies` (`package.json:6-60, 134-158`), using `@typescript/native-preview` (`tsgo --noEmit`) for its compiler gates (`package.json:8, 55, 56`). The proof command should use `tsgo --noEmit` or `pnpm lint`.
(confidence: 90)

VERDICT: REVISE


### lens-review-root-cause-r1.txt

[blocker] [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) — Deferring normalization of legacy `string[]` storage to "the next successful write" leaves the quota-exhausting legacy payload in `sessionStorage`, continuing to starve tab trees of shared storage capacity (confidence: 92)
* **Failure chain:** Under [f-20260906-24](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/tasks/findings.md#L7560), an unbounded string array in `sessionStorage["expanded-directories"]` exhausts the ~5 MB origin quota shared with tab trees. When an existing installation with a multi-megabyte legacy array starts up, [`Startup owner snapshot`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L14-L16) in [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/state/pathOwners.ts#L156-L163) does not rewrite storage. If atom hydration only truncates the in-memory representation while deferring physical storage normalization to "the next successful write", any session where the user studies games without expanding or collapsing a directory in [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/components/files/DirectoryTree.tsx#L62) leaves the bloated raw JSON in `sessionStorage`. Subsequent tab creations and tree updates will continue to throw unhandled `QuotaExceededError`. This patches the symptom in-memory for the directory tree while leaving the quota exhaustion mechanism intact in storage. Normalization and bounds enforcement must repair storage upon hydration (using the existing `repair` operation in [`src/state/utils.ts:34-42`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/state/utils.ts#L34-L42), as already contemplated in line 12's error handling) so shared capacity is reclaimed immediately.

[should-fix] [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) — In-memory expansion state is not explicitly reset or derived on workspace switch, allowing prior workspace capability IDs to contaminate the new workspace's storage record (confidence: 88)
* **Failure chain:** [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/state/atoms.ts#L132-L136) is currently a global atom decoupled from [`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/state/atoms.ts#L172). In [`DirectoryTree.tsx:156,167,180`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/components/files/DirectoryTree.tsx#L156-L180), expansion operations update state via `setExpanded((old) => [...old, handleKey])`. If the plan only makes the persisted record inactive without specifying that in-memory atom state is cleared or re-derived on workspace switch, `old` retains directory IDs from the prior workspace. The next directory expansion in the new workspace will merge the prior workspace's capability IDs with the new directory and persist that union under the new workspace ID. This defeats the stated isolation invariant ("Rejected: carry IDs across workspaces, because IDs are native capabilities from a different authority context") and leaks foreign capability IDs across workspaces. The plan must explicitly specify that in-memory expansion state is reset or derived from the active workspace on workspace switch.

[should-fix] [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) — Stale capability IDs for deleted or moved directories accumulate in storage and perpetuate across restarts without an eviction mechanism upon successful listing (confidence: 85)
* **Failure chain:** The plan specifies that "only IDs observed in the current listing may be admitted", but does not specify how or when existing stored IDs are reconciled against the listing. When directories are deleted or moved on disk, their IDs remain in `sessionStorage`. At startup, [`Startup owner snapshot`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L14-L16) must retain all bounded IDs matching the active workspace because no native listing has occurred yet. If [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/components/files/FilesPage.tsx#L88) does not reconcile and prune [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/src/state/atoms.ts#L132) against the successful `data` listing, dead IDs persist in `sessionStorage` and continue to be retained as capabilities by `Startup owner snapshot` across all future restarts, directly feeding into the stale capability accumulation in [`f-20260924-01`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r1/tasks/findings.md#L11231). The plan must specify that a successful workspace listing reconciles and evicts unobserved IDs from the active expansion state.

VERDICT: REVISE


### lens-review-tests-r1.txt

[blocker] src/components/files/DirectoryTree.test.tsx:109 — The test suite has no integration proof connecting workspace changes or storage write failures to tree expansion. In [DirectoryTree.test.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/src/components/files/DirectoryTree.test.tsx#L109), every test seeds `expandedDirectoriesAtom` into an in-memory Jotai store (`store.set(expandedDirectoriesAtom, [])`), never setting or modifying `fileWorkspaceAtom` and never exercising `sessionStorage`. In [FilesPage.test.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/src/components/files/FilesPage.test.tsx#L144), `DirectoryTree` is completely stubbed out (`vi.mock("./DirectoryTree", ...)`), and `FilesPage.test.tsx` is omitted from the Phase 1 proof command. If the UI implementation fails to switch expansion state on workspace change, or if a storage write failure in `setExpanded` throws an unhandled error crashing the component during user clicks, all existing and planned component tests would stay green. (confidence: 94)

[blocker] src/state/pathOwners.test.ts:200 — [pathOwners.test.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/src/state/pathOwners.test.ts#L200) only asserts the presence of IDs in `retainedIds` on healthy input, leaving malformed-state distrust, workspace-mismatch scoping, and pre-hydration non-mutation unguarded. If corrupt `expanded-directories` JSON is encountered, the plan requires withholding `"expandedDirectories"` from `trustedFamilies` so native startup reconciliation does not revoke valid capabilities. Mentally reverting this to return `{ retainedIds: [], trustedFamilies: [..., "expandedDirectories"] }` would pass any test asserting only on `retainedIds`. Furthermore, no planned assertion proves that `collectOriginalPathOwners` does not call `sessionStorage.setItem` to eagerly normalize legacy state before persisted atoms hydrate. (confidence: 93)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — The criterion "only IDs observed in the current listing may be admitted" is an addition not required by MANDATE (`f-20260906-24`) and has no verification in the plan. Bounding retention size/count and scoping identity to the active workspace are mandated, but filtering against the live directory listing is not. Neither [DirectoryTree.test.tsx](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/src/components/files/DirectoryTree.test.tsx) nor [pathOwners.test.ts](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/src/state/pathOwners.test.ts) asserts rejection of unobserved IDs. If implemented naively without guarding loading or empty SWR states (`data === undefined`), it introduces the `f-20260906-25` defect class (wiping stored preferences on initial load) with zero tests checking loading/error listing boundaries. (confidence: 88)

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Legacy normalization lacks a storage assertion ensuring that a legacy `string[]` is upgraded to the versioned record on write. If a test only asserts that an in-memory atom or helper returns the expanded string array after an update, a regression that continues persisting raw JSON `string[]` to `sessionStorage` would pass. The verification plan must explicitly assert that `sessionStorage.getItem("expanded-directories")` contains the new versioned record schema. (confidence: 86)

[nit] tasks/plans/2026-09-24-expanded-directory-storage.md:18 — The verification plan cites "Verify the actual file-tree behavior in the Tauri app; pixels use the pinned container", but Phase 1 proof omits `verify:app` and container e2e commands. Expanding directory nodes changes no visual layout or CSS requiring container screenshot diffing, and WebDriver cannot automate native GTK file pickers (`tauri.issueFileWorkspace()`, per [CLAUDE.md:217-220](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r1/CLAUDE.md#L217-L220)). Claiming browser/pixel coverage without a runnable command or specific test target is theater for a storage-layer change. (confidence: 85)

VERDICT: REVISE


### lens-review-correctness-r2.txt

I have initiated a search for the delta and snapshot files to examine the exact changes introduced in Round 2. I will inspect the findings as soon as the search completes.
### Lens: correctness of the changed code

#### Prior Substantive Correction Verification (Round 2)

- **P1: Listing-based admission unsupported** — **CLOSED**
  - *Correction verified:* Round 2 explicitly strikes listing-observed admission as a storage prerequisite ([tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9): *"The current listing is not an admission prerequisite"*). Storage hydration and snapshot inspection do not depend on asynchronous SWR listing resolution, eliminating the startup timing mismatch and unmandated UI coupling.
- **P2: Workspace-switch in-memory leak** — **CLOSED**
  - *Correction verified:* Round 2 specifies deriving the public `string[]` atom's value and functional updates against the active workspace ID ([tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9)). When switching workspaces, functional updater calls such as `setExpanded((old) => [...old, handleKey])` in [`DirectoryTree.tsx:156`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/components/files/DirectoryTree.tsx#L156) evaluate `old` against the active workspace handle (evaluating to `[]` for an unexpanded workspace), preventing capability IDs from a prior workspace from contaminating the new workspace record.
- **P3: Unbounded legacy storage remains on disk** — **CLOSED**
  - *Correction verified:* Round 2 removes the deferred write policy and specifies that valid oversized stored values and legacy arrays are physically repaired or migrated upon atom hydration after the startup snapshot ([tasks/plans/2026-09-24-expanded-directory-storage.md:9,12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L12)). This immediately reclaims storage quota without requiring user interaction in the directory tree.
- **P4: Legacy/malformed owner trust risk** — **CLOSED**
  - *Correction verified:* In the startup snapshot, unscoped legacy arrays, malformed JSON, and unreadable workspace identities are marked untrusted ([tasks/plans/2026-09-24-expanded-directory-storage.md:15](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L15)), preventing native startup reconciliation ([`mod.rs:6309-6312`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src-tauri/src/infra/path_authority/mod.rs#L6309-L6312)) from prematurely sweeping unowned directories. Valid records for a different or absent workspace retain zero IDs and remain trusted. Malformed storage is preserved without destructive overwrites, and the snapshot reader bypasses `collectOne`'s raw-equality check.
- **P5: Compression reader/writer mismatch** — **CLOSED**
  - *Correction verified:* Both the atom and the pre-hydration snapshot reader share [`src/state/expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/expandedDirectories.ts), standardizing on [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/store/debouncedStorage.ts#L7) for compression and [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/store/debouncedStorage.ts#L21) for decoding both compressed UTF-16 and legacy JSON ([tasks/plans/2026-09-24-expanded-directory-storage.md:12,15](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12-L15)).
- **P6: Oversized individual ID outcome undefined** — **CLOSED**
  - *Correction verified:* Round 2 explicitly resolves the logical contradiction regarding individual oversized IDs ([tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12)): an ID that exceeds the byte ceiling stays usable in-memory for the current session, but is not written to storage, does not claim durability, and reports a save failure through [`reportPreferenceStorageFailure("save", ...)`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/utils.ts#L19).
- **P7: Numeric bounds absent** — **CLOSED**
  - *Correction verified:* Exact numeric thresholds are established ([tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12)): maximum 1,000 IDs and maximum 64 KiB of UTF-8 JSON before compression. Both the atom writer and snapshot reader enforce identical limits.
- **P8: Proof command wrong or insufficient** — **CLOSED**
  - *Correction verified:* The proof command now uses the project's canonical compiler toolchain `pnpm exec tsgo --noEmit` ([tasks/plans/2026-09-24-expanded-directory-storage.md:34](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L34)), and verification explicitly requires asserting physical storage encoding and decoding to the versioned schema ([tasks/plans/2026-09-24-expanded-directory-storage.md:18](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18)).
- **P9: Generic helper/unneeded UI changes** — **CLOSED**
  - *Correction verified:* Phase 1 removes [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/components/files/DirectoryTree.tsx) and [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/components/files/FilesPage.tsx) edits, retains the public `string[]` contract on [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/atoms.ts#L132), introduces a domain-specific module [`src/state/expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/expandedDirectories.ts), and exports the existing error reporter from [`src/state/utils.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-correctness-r2/src/state/utils.ts#L19).

---

#### Evaluation of Changed Code and Plan (Round 2)

Mentally executing the revised state machine across all boundary conditions:
1. **Oversized and Bounded Eviction:** Newly expanded IDs are appended; when either count exceeds 1,000 or serialized UTF-8 exceeds 64 KiB, the oldest entries (head of array) are evicted until both limits hold. Collapse unconditionally removes the requested entry.
2. **Pre-hydration Snapshot Reader:** Evaluates without rewriting storage. Valid records for the matching workspace retain bounded IDs (`trusted: true`); valid records for a different or absent workspace retain 0 IDs (`trusted: true`); invalid schema, unscoped legacy arrays, and unreadable workspace identities return `trusted: false`, correctly protecting native entries during `reconcileStartupPathOwners`.
3. **Hydration & Storage Migration:** Oversized values and legacy arrays are bounded, converted to the versioned record with the active workspace ID, compressed via `serializeStorageValue`, and physically saved to storage without user clicks. Malformed storage is preserved and reported via `reportPreferenceStorageFailure("read", error)`.
4. **Workspace Isolation:** Public atom derivation ensures `setExpanded` functional updates always receive an array scoped to the active workspace ID, eliminating foreign capability leakage across workspace changes.

No correctness defects, inverted predicates, off-by-one bounds, or call-site contract mismatches were identified in the revised plan delta.

VERDICT: APPROVED


### lens-review-error-handling-r2.txt

### Prior substantive corrections (P1–P9) closure check

* **P1: Listing-based admission unsupported** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) explicitly establishes that "The current listing is not an admission prerequisite." Live directory listing synchronization has been excised from both atom hydration and startup snapshot contracts, resolving the asynchronous SWR timing defect.
* **P2: Workspace-switch in-memory leak** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) specifies deriving the public `string[]` atom and functional updates directly against the active opaque file-workspace ID, ensuring functional updates (`setExpanded(old => ...)`) operate only on the active workspace's state and cannot leak foreign capability IDs across workspaces.
* **P3: Unbounded legacy storage remains on disk** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:9,12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L12) establishes that legacy `string[]` is migrated and oversized valid records are physically repaired to bounded storage upon atom hydration immediately after the snapshot, immediately reclaiming quota without waiting for a user click.
* **P4: Legacy/malformed owner trust risk** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:9,12,15,28](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L28) guarantees that malformed records are not overwritten by a destructive empty repair, unscoped legacy arrays remain untrusted in the startup snapshot to prevent unowned capability cleanup, and the snapshot reader decouples normalized bounds from `collectOne`'s raw-equality check.
* **P5: Compression reader/writer mismatch** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:12,15](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12-L15) aligns both the atom writer and the pre-hydration snapshot reader on `serializeStorageValue` and `decodeCompressedOrJson`, sharing the concrete parsing logic in `src/state/expandedDirectories.ts`.
* **P6: Oversized individual ID outcome undefined** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12) explicitly specifies the failure contract: an individual ID too large to fit stays usable in memory for the session but is not persisted and triggers an explicit save failure reporting via `reportPreferenceStorageFailure("save", ...)`.
* **P7: Numeric bounds absent** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12) pins explicit limits: **1,000 IDs and 64 KiB of UTF-8 JSON before compression**.
* **P8: Proof command wrong or insufficient** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:18,34](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18-L34) replaces `tsc` with `pnpm exec tsgo --noEmit` and includes verification asserting physical storage bytes decode to the new versioned record.
* **P9: Generic helper/unneeded UI changes** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:34](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L34) eliminates UI component modifications, preserves the `string[]` contract on `DirectoryTree`, and scopes bounded storage to the concrete domain module `src/state/expandedDirectories.ts`.

---

### Findings

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:9 — Legacy migration failure path when active workspace is absent triggers false user-facing repair error (confidence: 88)
* **Failure chain:** [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) requires migrating legacy `string[]` to a versioned record immediately after the startup snapshot, noting that "A failed migration retains the raw legacy bytes and reports repair failure". However, if the application launches with no active workspace (`fileWorkspaceAtom` / `localStorage["file-workspace"]` is null, which is standard when no collection has been selected yet), there is no workspace ID to key the record to. If the migration routine treats an absent workspace as a migration failure and invokes [`reportPreferenceStorageFailure("repair", ...)`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r2/src/state/utils.ts#L19-L28), the user receives a red notification toast (`Common.PreferenceRepairFailed`) on a clean launch. The plan should specify that legacy migration requires a non-null active workspace ID; if the workspace is absent at startup, legacy state remains unmigrated (or inactive) without surfacing a false repair error notification to the user.

[should-fix] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Eviction loop ordering risks discarding existing valid storage entries before checking if an incoming ID exceeds the individual budget (confidence: 86)
* **Failure chain:** [tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12) specifies: "Validate nonempty unique IDs and evict oldest entries until both limits hold. Collapse always removes the ID. An individual ID too large to fit stays usable in memory but is not persisted and reports a save failure." If an implementation performs LRU eviction on the composite list before checking whether the newly appended candidate ID exceeds the 64 KiB ceiling by itself, the loop will evict all valid, historical entries from the record before determining that the candidate still cannot fit. The plan should specify that individual candidate ID size validation precedes LRU eviction, ensuring that an oversized ID fails admission without mutating or truncating previously persisted valid records.

[nit] tasks/plans/2026-09-24-expanded-directory-storage.md:12 — Untyped error cause when rejecting an oversized single ID risks degrading error message to generic fallback (confidence: 82)
* **Failure chain:** When an individual ID exceeds the 64 KiB budget, it reports a save failure via [`reportPreferenceStorageFailure("save", cause)`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r2/src/state/utils.ts#L19-L28). In [`src/state/storageError.ts:21-30`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r2/src/state/storageError.ts#L21-L30), `storageErrorCause` extracts `cause.message` if present; otherwise, it falls back to `Common.StorageRejected`. The plan should specify passing an explicit `Error` with a descriptive message (e.g. indicating the capability ID exceeds maximum storage budget) as the `cause` so the resulting toast provides diagnostic detail rather than the generic fallback.

VERDICT: APPROVED


### lens-review-minimalism-r2.txt

### Prior Substantive Correction Closure Status

* **P1: listing-based admission unsupported** — **CLOSED**. Line 9 explicitly removes live-listing coupling ("The current listing is not an admission prerequisite"), keeping [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/components/files/DirectoryTree.tsx) and [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/components/files/FilesPage.tsx) out of the admission path and eliminating unmandated scope expansion.
* **P2: workspace-switch in-memory leak** — **CLOSED**. Line 9 specifies deriving the public `string[]` atom value and functional updates against the active workspace ID in [`src/state/atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/atoms.ts), preventing stale in-memory IDs from contaminating new records without requiring changes to UI consumers.
* **P3: unbounded legacy storage remains on disk** — **CLOSED**. Lines 9 and 12 specify physical storage migration to the versioned record on atom hydration immediately after the startup snapshot, reclaiming session storage quota without waiting for user tree clicks.
* **P4: legacy/malformed owner trust risk** — **CLOSED**. Lines 9, 12, and 15 specify that unmigrated legacy arrays and malformed storage remain untrusted in [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/pathOwners.ts), while malformed bytes are preserved without destructive overwrite to keep native capability reconciliation fail-closed.
* **P5: compression reader/writer mismatch** — **CLOSED**. Lines 12 and 15 align both [`src/state/atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/atoms.ts) and [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/pathOwners.ts) on `serializeStorageValue` and `decodeCompressedOrJson` via the shared parsing logic.
* **P6: oversized individual ID outcome undefined** — **CLOSED**. Line 12 explicitly defines the contract: an oversized individual ID remains in memory for the active session but is rejected from persistent storage and reports a save failure.
* **P7: numeric bounds absent** — **CLOSED**. Line 12 defines concrete constants (**1,000 IDs and 64 KiB of UTF-8 JSON before compression**) shared across both atom and snapshot paths.
* **P8: proof command wrong or insufficient** — **CLOSED**. Line 34 replaces `tsc --noEmit` with `pnpm exec tsgo --noEmit` and Line 18 adds explicit assertions verifying physical storage byte decoding.
* **P9: generic helper / unneeded UI changes** — **CLOSED**. Phase 1 scopes the shared unit to a concrete domain module ([`src/state/expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/expandedDirectories.ts)) serving its two callers ([`src/state/atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/atoms.ts) and [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/pathOwners.ts)), exports the existing localized error reporter from [`src/state/utils.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/utils.ts) to eliminate duplicate message strings, and leaves UI components ([`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/components/files/DirectoryTree.tsx), [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/components/files/FilesPage.tsx)) untouched.

---

### Minimalism & Smallest Sufficient Diff Assessment

* **Minimum-Diff Counterfactual**: The proposed plan achieves the acceptance criteria with the smallest sufficient diff:
  1. Modifies 3 existing state files: [`src/state/atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/atoms.ts) (derives public `string[]` atom), [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/pathOwners.ts) (delegates snapshot parsing to the shared schema), and [`src/state/utils.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/utils.ts) (exports existing `reportPreferenceStorageFailure`).
  2. Adds 1 concrete domain module and test: [`src/state/expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-minimalism-r2/src/state/expandedDirectories.ts) (owning record schema, constants, and bounds enforcement). This has exactly two callers (`atoms.ts` and `pathOwners.ts`), preventing duplication between pre-hydration snapshot collection and Jotai atom evaluation while respecting startup initialization ordering.
  3. Eliminates all changes to UI components and drops unnecessary pixel container verification.
* No bloat, uncalled abstractions, configurability, or duplicate handlers remain.

VERDICT: APPROVED


### lens-review-persisted-state-r2.txt

# Persisted Renderer State and Tab Lifecycle Review (Round 2)

**Plan:** [2026-09-24-expanded-directory-storage.md](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md)
**Mandate:** `f-20260906-24`
**Scope:** Storage keys, atom families, serialization/deserialization encoding, quota bounding, workspace scoping, hydration, and startup capability snapshot agreement.

---

## Closure Status of Prior Substantive Corrections (P1–P9)

* **P1: Listing-based admission unsupported** — **CLOSED**
  Plan r2 line 9 explicitly removes the requirement for listing-observed admission ("The current listing is not an admission prerequisite"). Asynchronous directory enumeration via `tauri.listFileWorkspace` in `FilesPage` is decoupled from synchronous startup snapshot and atom hydration, preventing false wipes on reload.
* **P2: Workspace-switch in-memory leak** — **CLOSED**
  Plan r2 line 9 derives the public `string[]` atom's value and functional updates against `fileWorkspaceAtom`'s active opaque capability ID. Functional updates (`setExpanded((old) => [...old, handleKey])`) in `DirectoryTree` receive an empty array when switching to a new workspace, preventing foreign capability leakage across workspace boundaries.
* **P3: Unbounded legacy storage remains on disk** — **CLOSED**
  Plan r2 lines 9 and 12 specify that legacy `string[]` and valid oversized stored records are physically repaired/migrated to the bounded versioned schema on atom hydration immediately after the startup snapshot runs, reclaiming `sessionStorage` origin quota without requiring user interaction.
* **P4: Legacy/malformed owner trust risk** — **CLOSED**
  Plan r2 lines 12 and 15 ensure that malformed or unreadable data is never overwritten (preserving corrupt bytes for fail-closed native capability reconciliation) and leaves `expandedDirectories` untrusted. Unscoped legacy arrays are also treated as untrusted by the pre-hydration snapshot until properly migrated. Furthermore, normalized records bypass `collectOne`'s raw-equality check (`equal(read.value, parsed.data)`), preventing valid bounded arrays from being falsely rejected.
* **P5: Compression reader/writer mismatch** — **CLOSED**
  Plan r2 lines 12 and 15 unify serialization and deserialization across the persisted atom and `pathOwners.ts`: writes encode via `serializeStorageValue` (`compressToUTF16`), while reads on both sides decode via `decodeCompressedOrJson` (transparent decompression with legacy JSON fallback).
* **P6: Oversized individual ID outcome undefined** — **CLOSED**
  Plan r2 line 12 explicitly defines the contract: an individual directory capability ID whose serialized length alone exceeds the byte budget remains usable in memory for the active session, is excluded from persistence, and reports a save failure through `reportPreferenceStorageFailure("save", error)`.
* **P7: Numeric bounds absent** — **CLOSED**
  Plan r2 line 12 establishes explicit numeric caps: maximum 1,000 IDs and maximum 64 KiB of UTF-8 JSON before compression. This bounds storage usage to ~15–20 KiB compressed UTF-16 (< 0.5% of the ~5 MB origin quota shared with tab trees) and keeps startup capability input well within native `MAX_AUTHORITY_IDS` (4096).
* **P8: Proof command wrong or insufficient** — **CLOSED**
  Plan r2 lines 18 and 34 replace `tsc --noEmit` with `tsgo --noEmit` and include explicit assertions that raw `sessionStorage` bytes decode to the new compressed versioned record schema.
* **P9: Generic helper/unneeded UI changes** — **CLOSED**
  Plan r2 line 34 restricts Phase 1 changes to `src/state/atoms.ts`, `src/state/expandedDirectories.ts`, `src/state/pathOwners.ts`, and `src/state/utils.ts` (exporting `reportPreferenceStorageFailure`), leaving `DirectoryTree.tsx` and `FilesPage.tsx` untouched and maintaining the tree component's existing `string[]` contract.

---

## Lens Analysis: Persisted Renderer State & Tab Lifecycle

1. **Writer and reader agreement:**
   Both the persisted atom in `atoms.ts` and the pre-hydration snapshot reader in `pathOwners.ts` import from the shared domain module `src/state/expandedDirectories.ts`. Both read the `"expanded-directories"` key in `sessionStorage` via `decodeCompressedOrJson`. Writes consistently compress with `serializeStorageValue`.
2. **Origin quota & tab tree safety:**
   The 64 KiB UTF-8 JSON / 1,000 ID cap compresses to under 20 KiB in `sessionStorage`, safely coexisting with tab tree stores in the shared ~5 MB quota. Failed writes (e.g. `QuotaExceededError`) are caught and routed to `reportPreferenceStorageFailure`, leaving tab trees and practice history untouched.
3. **Key and identity scoping:**
   Expansion state is scoped to the opaque native workspace capability ID (`PathRef.id`), never user-editable path strings. Switching workspaces safely derives an empty active selection without destroying dormant records or leaking capabilities.
4. **Hydration & hostile data resilience:**
   Absent keys safely yield empty defaults; corrupted or truncated inputs are reported via `reportPreferenceStorageFailure("read", error)` without executing destructive empty writes; pre-hydration snapshot fails closed to untrusted for unreadable workspace identity, corrupt data, or unmigrated legacy arrays.
5. **Tab lifecycle & debounced writes:**
   Storage operations remain synchronous, with no deferred debounce flush requirements.

No remaining defects or blockers within this lens.

---

VERDICT: APPROVED


### lens-review-plan-r2.txt

### Prior Substantive Corrections (Closure Status)

- **P1: listing-based admission unsupported** — **CLOSED**
  The unmandated requirement that "only IDs observed in the current listing may be admitted" was eliminated ([`2026-09-24-expanded-directory-storage.md:9`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9)). The plan now explicitly affirms: "The current listing is not an admission prerequisite," preventing startup wiping when asynchronous SWR listings ([`src/components/files/FilesPage.tsx:86-90`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/FilesPage.tsx#L86-L90)) have not yet resolved.
- **P2: workspace-switch in-memory leak** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:9,18`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9). The public `string[]` atom value and its functional updates (`(old) => ...`) are derived against [`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/atoms.ts#L172). On a workspace switch, `old` reflects the active workspace state rather than accumulating directory capabilities from prior workspaces.
- **P3: unbounded legacy storage remains on disk** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:9,12`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L12). The plan specifies that valid legacy arrays and oversized stored records are physically repaired/migrated to the compressed versioned format upon atom hydration immediately following the pre-hydration startup snapshot. This reclaims shared origin quota without requiring a user interaction click in [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/DirectoryTree.tsx#L62).
- **P4: legacy/malformed owner trust risk** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:9,12,15,28`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L28). Unscoped legacy arrays lack workspace ownership and are treated as untrusted by [`Startup owner snapshot`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.ts#L156). Malformed storage is preserved without destructive default overwriting, reported as a read failure, and left untrusted. Normalization bypasses [`collectOne`'s raw-equality check](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.ts#L56). Records for inactive workspaces retain 0 IDs while remaining trusted.
- **P5: compression reader/writer mismatch** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:12,15`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12-L15). Persistence routes through [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/store/debouncedStorage.ts#L7) while reads on both the atom side and pre-hydration snapshot side use [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/store/debouncedStorage.ts#L21) from the shared concrete parser in `src/state/expandedDirectories.ts`.
- **P6: oversized individual ID outcome undefined** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:12`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12). The failure contract is explicitly specified: an individual ID too large to fit within the 64 KiB ceiling remains active in-memory for the session but is withheld from persistence and reported as an undurable preference save failure.
- **P7: numeric bounds absent** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:12`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12). Exact numeric limits are stated as **1,000 IDs and 64 KiB of UTF-8 JSON before compression**, satisfying `.claude/rules/async-resource-invariants.md:59` and `.claude/rules/persisted-state.md:21-24`.
- **P8: proof command wrong or insufficient** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:18,34`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18-L34). The proof command specifies `pnpm exec tsgo --noEmit` matching the project toolchain ([`package.json:8`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/package.json#L8)), and verification explicitly requires asserting storage encoding, owner snapshot distrust, legacy migration, and boundary thresholds.
- **P9: generic helper/unneeded UI changes** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:34`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L34). Scope is restricted to the concrete domain module `src/state/expandedDirectories.ts` alongside [`atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/atoms.ts), [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.ts), and exporting the existing error helper from [`utils.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/utils.ts#L19). UI files [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/DirectoryTree.tsx) and [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/FilesPage.tsx) are preserved untouched.

---

### Plan Review Findings

No blockers, should-fixes, or nits were found against the candidate plan revision.

1. **Named Symbols:** All referenced existing symbols ([`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/atoms.ts#L172), [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/store/debouncedStorage.ts#L7), [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/store/debouncedStorage.ts#L21), [`collectOne`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.ts#L48), [`reportPreferenceStorageFailure`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/utils.ts#L19)) exist in source and behave as described. Exporting `reportPreferenceStorageFailure` from `src/state/utils.ts` is explicitly listed under Phase 1.
2. **Consumers of Changed Contracts:** Direct consumers of `expandedDirectoriesAtom` are limited to [`DirectoryTree.tsx:62`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/DirectoryTree.tsx#L62) and [`DirectoryTree.test.tsx:5,109`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/DirectoryTree.test.tsx#L5). The atom's public signature (`string[]`) is maintained. Storage consumers for `"expanded-directories"` are strictly [`atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/atoms.ts#L132) and [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.ts#L159), both of which are updated to share `expandedDirectories.ts`.
3. **Repository Invariants:** Complies with `.claude/rules/persisted-state.md` (compression with `serializeStorageValue`, bounds before write and on hydration, handled quota failure, no user-editable path identity) and `.claude/rules/async-resource-invariants.md` (bounded accumulation to 1,000 IDs / 64 KiB, fail-closed startup snapshot).
4. **Edge Cases:** Unreadable/corrupted storage, absent storage, absent active workspace, mismatched workspace records, single oversized ID, and legacy unmigrated payloads are accounted for with explicit trust/retention rules.
5. **Phase & Verification:** Phase 1 cleanly decomposes the state domain without touching UI components. The proof command is runnable and test assertions guard storage encoding, migration, and trust boundaries.

---

### Limitations

- **Throwaway worktree missing `node_modules` (confidence: 90):** Executing `pnpm exec vitest` directly inside the throwaway worktree was prevented because dependencies are not installed in this isolated build worktree. All assertions, symbol contracts, mock topologies ([`FilesPage.test.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/FilesPage.test.tsx#L23), [`DirectoryTree.test.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/components/files/DirectoryTree.test.tsx#L107), [`pathOwners.test.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/src/state/pathOwners.test.ts#L200)), and command configurations ([`package.json`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r2/package.json#L8)) were verified via static source tracing and git history.

VERDICT: APPROVED


### lens-review-root-cause-r2.txt

# Root Cause vs. Symptom Review: Bounded Expanded-Directory Storage (`f-20260906-24`)

## Analysis: Root Cause vs. Symptom

### 1. The Original Failure Mechanism ([`f-20260906-24`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/tasks/findings.md#L7560))
* **State at fault:** An unbounded `string[]` array serialized as raw JSON in `sessionStorage["expanded-directories"]` without workspace attribution or storage budgeting.
* **Mechanism:**
  1. `DirectoryTree` appends expanded node keys to [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L132-L136) on every folder expansion without count caps or size constraints.
  2. Because [`createZodStorage`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/utils.ts#L30-L80) writes uncompressed JSON, large directory structures rapidly expand, consuming significant portions of the shared ~5 MB `sessionStorage` origin quota.
  3. This quota starvation triggers `QuotaExceededError` on subsequent tab creations and tree modifications in [`debouncedStorage.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/store/debouncedStorage.ts), risking data loss for active game and analysis trees.
  4. In addition, the storage key lacked workspace scoping. Startup capability reconciliation in [`collectOriginalSnapshots`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/pathOwners.ts#L156-L163) collected all stored IDs indiscriminately into `retainedIds`, causing stale directory handles across prior workspaces to accumulate indefinitely and risking startup `ResourceLimit` errors ([`f-20260924-01`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/tasks/findings.md#L11229-L11236)).

### 2. Root-Cause Evaluation of the R2 Plan
The Round 2 candidate plan ([`2026-09-24-expanded-directory-storage.md`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md)) dismantles each link in the failure chain at its root rather than patching symptoms:
* **Quota starvation elimination:** Explicit numeric bounds (**1,000 IDs and 64 KiB UTF-8 JSON before compression**) coupled with UTF-16 compression via [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/store/debouncedStorage.ts#L14) guarantee that expansion storage consumes < 0.3% of the shared 5 MB quota.
* **Immediate quota reclamation:** Rather than deferring legacy migration to an indefinite user write, valid legacy or oversized data is physically repaired on disk during atom hydration immediately after the startup snapshot. This reclaims shared capacity at launch even if the user never interacts with the directory tree.
* **Workspace capability isolation:** Persisting a versioned record `{ version: 1, workspaceId, ids }` and re-deriving the public atom's value and functional updates against [`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L172-L176) stops cross-workspace ID leakage both in memory and on disk.
* **Fail-closed capability protection:** Malformed records and unscoped legacy arrays are marked untrusted in the startup snapshot without executing destructive overwrites (`[]`), preserving native fail-closed capability reconciliation in [`reconcile_startup_owners`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src-tauri/src/infra/path_authority/mod.rs#L6253-L6320).
* **Truthful failure semantics:** An individual ID exceeding 64 KiB is kept in memory for transient UI responsiveness but refused persistence with an explicit save failure report via [`reportPreferenceStorageFailure`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/utils.ts#L19), avoiding silent data dropping.

---

## Prior Fix Check

A review of `git log` and `git blame` for [`src/state/atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L132) and [`src/state/pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/pathOwners.ts#L156) reveals:
* [`expandedDirectoriesAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L132) was introduced in commit `9361211b` without prior budgeting or scoping changes.
* Capability collection in [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/pathOwners.ts) was added under `5e8db423` as a raw JSON consumer.
* No prior fix for [`f-20260906-24`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/tasks/findings.md#L7560) has been previously attempted or reverted. The R2 plan introduces no regression of previously established invariants.

---

## Closure Status of Prior Substantive Corrections

| Issue ID | Description & Round 1 Defect | Round 2 Resolution & Root Cause Verification | Status |
| :--- | :--- | :--- | :--- |
| **P1** | **Listing-based admission unsupported**<br>Coupling hydration to unobserved directory listings would wipe stored expansion state before asynchronous IPC resolved. | Struck entirely: `"The current listing is not an admission prerequisite."` Admission bounds apply strictly to storage schema, count, and byte budgets. | **CLOSED** |
| **P2** | **Workspace-switch in-memory leak**<br>Independent atom allowed `setExpanded((old) => [...old, key])` to preserve and persist prior workspace IDs under the new workspace key. | Derived atom resolves active workspace from [`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L172) on every read and functional update, isolating state across workspace switches without touching UI components. | **CLOSED** |
| **P3** | **Unbounded legacy storage remains on disk**<br>Deferring legacy normalization to "next write" left multi-megabyte payloads in `sessionStorage` indefinitely during read-only sessions. | Atom hydration physically repairs/migrates legacy or oversized valid storage to the versioned record immediately after the startup snapshot, reclaiming quota without requiring user clicks. | **CLOSED** |
| **P4** | **Legacy/malformed owner trust risk**<br>`collectOne`'s strict raw-equality check distrusted normalized records; destructive repair erased corrupt records; unscoped legacy was at risk of misattribution. | Pre-hydration reader bypasses `collectOne` raw-equality check; unscoped legacy and corrupt records are marked untrusted (fail-closed); records for absent/mismatched workspaces retain 0 IDs and are trusted; malformed raw bytes are preserved. | **CLOSED** |
| **P5** | **Compression reader/writer mismatch**<br>Atom writer used uncompressed JSON while persisted-state rules mandated UTF-16 compression, causing `SyntaxError` in `readJson` at snapshot. | Concrete [`src/state/expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/expandedDirectories.ts) coordinates writes through [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/store/debouncedStorage.ts#L14) and reads through [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/store/debouncedStorage.ts#L12) across both atom and snapshot paths. | **CLOSED** |
| **P6** | **Oversized individual ID outcome undefined**<br>Claiming to fit a single ID larger than the byte budget created a logical contradiction between hard limits and silent dropping. | Oversized single IDs (> 64 KiB) remain in-memory for transient session interaction but are excluded from disk persistence and explicitly report a save failure. | **CLOSED** |
| **P7** | **Numeric bounds absent**<br>Unstated count and byte limits left retention thresholds open to divergent implementations. | Explicit constants specified: **1,000 IDs** and **64 KiB of UTF-8 JSON before compression**. | **CLOSED** |
| **P8** | **Proof command wrong or insufficient**<br>Used non-existent `tsc --noEmit` and lacked verification of physical storage bytes and schema migration. | Phase 1 proof specifies `pnpm exec tsgo --noEmit`; verification explicitly mandates asserting physical storage bytes decode to the new versioned record. | **CLOSED** |
| **P9** | **Generic helper/unneeded UI changes**<br>Premature generic storage abstraction and unnecessary modifications to [`FilesPage.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/components/files/FilesPage.tsx) and [`DirectoryTree.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/components/files/DirectoryTree.tsx). | Scoped strictly to concrete domain module [`expandedDirectories.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/expandedDirectories.ts), [`atoms.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts), [`pathOwners.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/pathOwners.ts), and exporting the existing error reporter in [`utils.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/utils.ts#L19); UI components remain unchanged. | **CLOSED** |

---

## Limitations

* **Native GTK File Picker in E2E:** Interactive workspace switching cannot be automated via WebKitGTK WebDriver because [`tauri.issueFileWorkspace()`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/components/files/FilesPage.tsx#L103) invokes an OS-native GTK file chooser ([`CLAUDE.md:217-220`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/CLAUDE.md#L217-L220)). Full end-to-end verification of workspace switching relies on unit/integration testing in Vitest by programmatically seeding [`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-root-cause-r2/src/state/atoms.ts#L172). (confidence: 95)

---

VERDICT: APPROVED


### lens-review-tests-r2.txt

### Prior Substantive Corrections Closure Status

* **P1 (Listing-based admission unsupported):** **CLOSED.** Plan line 9 explicitly states: *"The current listing is not an admission prerequisite."* Verification removes unmandated listing synchronization requirements.
* **P2 (Workspace-switch in-memory leak):** **CLOSED.** Plan line 9 specifies deriving the public `string[]` atom against the active workspace ID. Verification requires testing *"active workspace switching including functional updates"*, proving that switching workspaces prevents prior-workspace capability IDs from leaking into functional updates (`setExpanded(old => [...old, ...])`).
* **P3 (Unbounded legacy storage remains on disk):** **CLOSED.** Plan line 12 specifies physical repair on atom hydration after the startup snapshot. Verification explicitly mandates: *"Cover legacy hydration and physical migration... Assert physical storage bytes decode to the new versioned record."*
* **P4 (Legacy/malformed owner trust risk):** **CLOSED.** Plan line 15 specifies that unscoped legacy arrays, unreadable workspace identity, and malformed JSON remain untrusted, while valid records matching the active workspace (or valid records for other workspaces retaining 0 IDs) remain trusted, without passing through `collectOne` raw-equality. Verification requires testing startup snapshot trust, mismatch, and read-only non-mutation behavior.
* **P5 (Compression reader/writer mismatch):** **CLOSED.** Plan line 12 specifies `serializeStorageValue` for writes and `decodeCompressedOrJson` for reading both compressed versioned records and legacy raw JSON. Verification asserts physical storage decodes to the versioned record.
* **P6 (Oversized individual ID outcome undefined):** **CLOSED.** Plan line 12 defines the failure contract: an individual ID too large to fit stays usable in memory for the session, is not persisted to storage, and reports a save failure through `reportPreferenceStorageFailure("save", ...)`. Verification explicitly includes *"both exact bounds and an oversized single ID, failed writes/repairs"*.
* **P7 (Numeric bounds absent):** **CLOSED.** Concrete bounds are stated in line 12: **1,000 IDs and 64 KiB of UTF-8 JSON before compression**. Verification mandates testing both exact bounds.
* **P8 (Proof command wrong or insufficient):** **CLOSED.** Proof command in line 34 specifies `pnpm exec vitest run src/components/files/DirectoryTree.test.tsx src/state/pathOwners.test.ts src/state/expandedDirectories.test.ts` and `pnpm exec tsgo --noEmit` (the project's native compiler check).
* **P9 (Generic helper / unneeded UI changes):** **CLOSED.** The plan defines a domain-specific module (`src/state/expandedDirectories.ts`), keeps the `string[]` atom interface intact, and makes zero edits to `DirectoryTree.tsx` or `FilesPage.tsx`.

---

### Findings

[should-fix] [tasks/plans/2026-09-24-expanded-directory-storage.md:18](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18) — Asserting physical storage bytes solely via `decodeCompressedOrJson` creates a self-defeating decode anchor for compression; mentally reverting `serializeStorageValue` to `JSON.stringify` would still pass because `decodeCompressedOrJson` transparently falls back to raw JSON parsing. The verification assertion must explicitly assert that `deserializeStorageValue(sessionStorage.getItem("expanded-directories")!) !== null` (or that `JSON.parse` throws a `SyntaxError` on the raw compressed UTF-16 string) to prove compression compliance with [`.claude/rules/persisted-state.md:21-24`](file:///home/felixb/Projekte/chessfable/.claude/rules/persisted-state.md#L21-L24). (confidence: 88)

[should-fix] [src/state/pathOwners.test.ts:200](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r2/src/state/pathOwners.test.ts#L200) — Existing test in `pathOwners.test.ts:200` seeds an unscoped legacy array (`["expanded"]`) and asserts it is trusted (`expect(owners.trustedFamilies).toHaveLength(11)`) and retained (`expect(owners.retainedIds).toContain("expanded")`). Under Plan R2 ("the startup snapshot treats unmigrated legacy as untrusted, so it never attributes its IDs to an arbitrary workspace"), this existing test will fail unless updated to the versioned record schema. Furthermore, to guard the fix in line 15 ("does not pass normalized data through `collectOne`'s raw-equality check"), the snapshot test suite must test an oversized record requiring bounds normalization (e.g. > 1,000 IDs) and assert trust is retained; testing only small records (< 1,000 IDs) would stay green even if `collectOne`'s raw-equality check (`equal(read.value, parsed.data)`) was mistakenly reintroduced. (confidence: 86)

[nit] [src/state/expandedDirectories.test.ts:1](file:///home/felixb/Projekte/chessfable/src/state/expandedDirectories.test.ts#L1) — Integration proof for `expandedDirectoriesAtom` functional updates across workspace switches must verify through a Jotai store instance (`createStore()`) with real `fileWorkspaceAtom` transitions, rather than testing isolated helper functions in `expandedDirectories.ts`. A test exercising only isolated pure helper functions would leave the atom derivation in `src/state/atoms.ts` unguarded against functional update stale-closure regressions. (confidence: 82)

---

### Revert Test Analysis for Central Behaviors

1. **Workspace boundary isolation across functional updates:**
   * *Behavior:* Switching `fileWorkspaceAtom` from `ws1` to `ws2` ensures `setExpanded((old) => [...old, "dirB"])` does not receive `dirA` from `ws1`.
   * *Revert outcome:* Mentally reverting the derived atom to the existing un-scoped `atomWithStorage` causes `old` to contain `["dirA"]`, saving `["dirA", "dirB"]` under `ws2`. The assertion `expect(store.get(expandedDirectoriesAtom)).toEqual(["dirB"])` and inspecting `sessionStorage` record for `ws2` fails red.
2. **Quota reclamation via physical migration on hydration:**
   * *Behavior:* Seeding an oversized legacy array in `sessionStorage` is repaired upon atom hydration after the startup snapshot.
   * *Revert outcome:* Mentally reverting the physical repair to leave legacy bytes untouched until a user click leaves raw JSON in `sessionStorage`. The assertion `expect(sessionStorage.getItem("expanded-directories")).not.toBe(rawLegacy)` fails red.
3. **Preserving malformed bytes fail-closed:**
   * *Behavior:* Corrupt JSON in `sessionStorage` leaves the owner family untrusted in `pathOwners.ts` and is not overwritten with `[]`.
   * *Revert outcome:* Mentally reverting to `createZodStorage` default repair overwrites `sessionStorage` with `[]` and marks the family trusted on subsequent startup. The assertions `expect(owners.trustedFamilies).not.toContain("expandedDirectories")` and `expect(sessionStorage.getItem("expanded-directories")).toBe(corruptRaw)` fail red.

VERDICT: APPROVED


### lens-review-error-handling-r3.txt

### Prior substantive corrections and open issues closure check (P10–P13, P1–P9)

* **P10: Absent-workspace migration should not report false repair failure** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9) explicitly specifies: *"Without an active workspace, expose no expansions and leave legacy bytes untouched without a spurious repair error."* Startup when `fileWorkspaceAtom` is `null` avoids invoking [`reportPreferenceStorageFailure("repair", ...)`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r3/src/state/utils.ts#L19-L28), eliminating the false `Common.PreferenceRepairFailed` notification toast on clean launches.
* **P11: Oversize incoming ID must be rejected before evicting valid entries** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:12](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12) explicitly mandates: *"Reject an individually oversized incoming ID before evicting prior valid entries; keep it usable in memory, leave the last good persisted record intact, and report a save failure with a concrete size cause."* The individual budget check precedes the LRU eviction loop, ensuring valid historical entries in `sessionStorage` are not purged when a candidate expansion exceeds the 64 KiB limit by itself. Furthermore, specifying a concrete size cause ensures [`storageErrorCause`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-error-handling-r3/src/state/storageError.ts#L21-L30) passes a diagnostic message rather than degrading to generic fallback.
* **P12: Compression test must use deserializeStorageValue, not dual decoder** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:19](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L19) amends Verification: *"Assert physical storage bytes decode to the new versioned record through `deserializeStorageValue`, so a raw JSON writer would fail the test."*
* **P13: Old owner test must change and oversized record snapshot must prove trusted normalization** — **CLOSED**.
  * *Evidence:* [tasks/plans/2026-09-24-expanded-directory-storage.md:19](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L19) updates Verification: *"Update the old owner test that expects unscoped legacy IDs to be trusted; add an oversized-record snapshot test that proves normalization does not make the family untrusted."*
* **P1–P9 (Round 1/2 substantive issues)** — **CONFIRMED CLOSED**.
  * Live listing synchronization remains excluded (P1); workspace scoping prevents cross-workspace capability leakage (P2); active workspace hydration immediately reclaims oversized storage (P3); malformed records are preserved without empty overwrites and legacy arrays remain untrusted at snapshot (P4); serialization and decompression are symmetric (P5); single oversized ID handling is specified (P6); exact limits (1,000 IDs / 64 KiB) are pinned (P7); proof commands and verification assertions are sound (P8); and changes remain scoped to state domain modules without extraneous UI modifications (P9).

---

### Failure path evaluation

1. **Storage Read Failure (malformed / corrupt data):**
   * *Trigger:* `sessionStorage["expanded-directories"]` contains unparseable or schema-violating data.
   * *Concrete Outcome:* Atom hydration catches the defect, preserves raw storage bytes without destructive overwriting (`write(key, initialValue, "repair")` from `createZodStorage` is avoided), surfaces a red toast via `reportPreferenceStorageFailure("read", error)`, falls back to `[]` in memory to prevent React rendering crashes, and leaves `expandedDirectories` untrusted during the startup snapshot so native reconciliation fails closed without unverified cleanup.
2. **Storage Migration / Repair Failure:**
   * *Trigger:* Legacy data or oversized valid records cannot be written back (e.g. `sessionStorage` quota exceeded).
   * *Concrete Outcome:* The existing raw bytes are retained; `reportPreferenceStorageFailure("repair", cause)` emits a `Common.PreferenceRepairFailed` notification toast with the underlying failure reason; in-memory expansion remains usable for the active session; tab trees and practice history are never evicted.
3. **Storage Save Failure (individual oversized candidate or quota exhaustion):**
   * *Trigger:* An expanded directory ID exceeds 64 KiB individually, or storage runs out of space on write.
   * *Concrete Outcome:* For an oversized ID, rejection occurs before evicting valid entries; the last good persisted record remains untouched; `reportPreferenceStorageFailure("save", cause)` displays `Common.PreferenceSaveFailed` with a concrete size message; durability is not claimed; in-memory expansion stays responsive in the UI without crashing.
4. **Absent Workspace Startup:**
   * *Trigger:* Application launched with no active file workspace.
   * *Concrete Outcome:* Atom exposes `[]`, legacy bytes remain untouched in storage, and no false repair failure toast is shown. Startup snapshot marks unscoped legacy arrays untrusted, safeguarding native capabilities.

No unhandled failure paths, swallowed exceptions, or false-success degradations remain.

---

VERDICT: APPROVED


### lens-review-plan-r3.txt

### Substantive Corrections (Closure Status)

- **P10: absent-workspace migration should not report false repair failure** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:9`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9). The candidate revision specifies: *"Without an active workspace, expose no expansions and leave legacy bytes untouched without a spurious repair error."* This aligns with the settled startup snapshot contract in [`pathOwners.ts:156-163`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.ts#L156-L163) (which treats unscoped legacy arrays as untrusted and an absent workspace as retaining zero IDs and trusted), preventing false `reportPreferenceStorageFailure("repair", ...)` alerts when no file workspace capability exists to key a migrated record.
- **P11: oversize incoming ID must be rejected before evicting valid entries** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:12`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L12). The plan specifies: *"Reject an individually oversized incoming ID before evicting prior valid entries; keep it usable in memory, leave the last good persisted record intact, and report a save failure with a concrete size cause."* This prevents an eviction loop from purging legitimate persisted entries when a single candidate ID exceeds the 64 KiB limit before compression, keeping the last good record intact while allowing in-memory expansion for the active session.
- **P12: compression test must use deserializeStorageValue, not dual decoder** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:18`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18). The verification obligation now mandates: *"Assert physical storage bytes decode to the new versioned record **through `deserializeStorageValue`**, so a raw JSON writer would fail the test."* Because [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/store/debouncedStorage.ts#L21-L30) falls back to uncompressed `JSON.parse`, testing through [`deserializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/store/debouncedStorage.ts#L11-L18) guarantees that raw JSON writes fail decompression and would turn the test red.
- **P13: old owner test must change and oversized record snapshot must prove trusted normalization** — **CLOSED**
  Addressed in [`2026-09-24-expanded-directory-storage.md:18`](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L18). The plan explicitly mandates updating the pre-existing test in [`src/state/pathOwners.test.ts:200`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.test.ts#L200) (which previously expected unscoped legacy IDs to be trusted) and adding an oversized-record snapshot test proving that normalization down to 1,000 IDs / 64 KiB does not mark the family untrusted under [`collectOne`'s strict raw-equality check](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.ts#L56).

### Prior Corrections (P1–P9)
All prior substantive corrections ([P1 through P9](file:///home/felixb/Projekte/chessfable/tasks/plans/2026-09-24-expanded-directory-storage.md#L9-L34)) remain verified, intact, and explicitly closed.

---

### Plan Review Findings

No blockers, should-fixes, or nits were found against the round 3 candidate plan.

1. **Named Symbols:** All referenced existing symbols ([`fileWorkspaceAtom`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/atoms.ts#L172), [`serializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/store/debouncedStorage.ts#L7), [`deserializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/store/debouncedStorage.ts#L11), [`decodeCompressedOrJson`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/store/debouncedStorage.ts#L21), [`collectOne`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.ts#L48), [`reportPreferenceStorageFailure`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/utils.ts#L19)) exist in source and match the contract.
2. **Consumers of Changed Contracts:** All consumers of `expandedDirectoriesAtom` and storage key `"expanded-directories"` ([`DirectoryTree.tsx:62`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/components/files/DirectoryTree.tsx#L62), [`DirectoryTree.test.tsx:109`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/components/files/DirectoryTree.test.tsx#L109), [`atoms.ts:132`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/atoms.ts#L132), [`pathOwners.ts:159`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.ts#L159), [`pathOwners.test.ts:200`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.test.ts#L200)) are fully accounted for. The public atom signature remains `string[]`.
3. **Repository Invariants:** Conforms to `.claude/rules/persisted-state.md` and `.claude/rules/async-resource-invariants.md`: bounded retention to 1,000 IDs and 64 KiB UTF-8 JSON before compression, storage writes compressed with `serializeStorageValue`, truthful handled failures, and no user-editable path identity.
4. **Edge Cases:** Properly handles absent workspace, workspace switching, single oversized ID, oversized stored record, legacy unmigrated payloads, and malformed storage preservation.
5. **Phase & Verification:** Phase 1 cleanly decomposes the sensitive state domain. The proof command `pnpm exec vitest run src/components/files/DirectoryTree.test.tsx src/state/pathOwners.test.ts src/state/expandedDirectories.test.ts` and `pnpm exec tsgo --noEmit` verifies both runtime behavior and TypeScript typing across the modified files.

---

### Limitations

- **Isolated worktree without `node_modules` (confidence: 90):** Package dependencies are not installed inside `/tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3`, preventing direct execution of `vitest` or `tsgo`. All assertions, symbol contracts, mock topologies ([`DirectoryTree.test.tsx`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/components/files/DirectoryTree.test.tsx#L107), [`pathOwners.test.ts`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/src/state/pathOwners.test.ts#L200)), and command definitions ([`package.json:8,35`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-plan-r3/package.json#L8)) were verified via static source tracing and git history.

VERDICT: APPROVED


### lens-review-tests-r3.txt

### Substantive Corrections Closure Status

* **P10 (Absent-workspace migration false repair failure):** **CLOSED.** Plan line 9 specifies: *"Without an active workspace, expose no expansions and leave legacy bytes untouched without a spurious repair error."* Verification in line 18 mandates covering *"absent and active workspace switching including functional updates through a real Jotai store ... failed writes/repairs"*. A test initializing the store with legacy bytes in `sessionStorage` and `fileWorkspaceAtom = null` proves that `expandedDirectoriesAtom` exposes `[]`, `sessionStorage` retains the untouched legacy bytes, and no spurious repair failure is reported.
* **P11 (Oversized incoming ID eviction ordering):** **CLOSED.** Plan line 12 specifies: *"Reject an individually oversized incoming ID before evicting prior valid entries; keep it usable in memory, leave the last good persisted record intact, and report a save failure with a concrete size cause."* Verification in line 18 explicitly tests *"both exact bounds and an oversized single ID, failed writes/repairs"*. A test expanding a single ID > 64 KiB against an atom with pre-existing valid entries proves that in-memory state retains the new ID, persisted storage retains the prior valid record without evicting existing entries, and `reportPreferenceStorageFailure("save", ...)` is called with a concrete size error.
* **P12 (Compression decode anchor self-defeating):** **CLOSED.** Plan line 18 mandates: *"Assert physical storage bytes decode to the new versioned record through `deserializeStorageValue`, so a raw JSON writer would fail the test."* Because [`deserializeStorageValue`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r3/src/state/store/debouncedStorage.ts#L11-L18) strictly checks for compressed UTF-16 strings and returns `null` for raw JSON, mentally reverting `serializeStorageValue` to `JSON.stringify` turns the assertion red.
* **P13 (Old owner test update & oversized record snapshot trust):** **CLOSED.** Plan line 18 specifies: *"Update the old owner test that expects unscoped legacy IDs to be trusted; add an oversized-record snapshot test that proves normalization does not make the family untrusted."* Updating [`src/state/pathOwners.test.ts:200`](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r3/src/state/pathOwners.test.ts#L200) to the versioned schema prevents broken tests once unmigrated legacy is untrusted, and adding a snapshot test with > 1,000 IDs guards against reintroducing [`collectOne`'s raw-equality check](file:///tmp/build-df96e5f3-0928-4d01-8c96-356e430dceb7/wt-lens-review-tests-r3/src/state/pathOwners.ts#L56) (`!equal(read.value, parsed.data)`), failing red if normalization marks the family untrusted.

---

### Findings

[nit] [src/state/expandedDirectories.test.ts:1](file:///home/felixb/Projekte/chessfable/src/state/expandedDirectories.test.ts#L1) — The test for an individually oversized incoming ID must seed non-empty prior valid entries in `sessionStorage` before attempting admission; testing from an initially empty state would fail to catch an implementation regression where eviction runs before the oversize check (both would end with empty storage). The test must assert that `deserializeStorageValue(sessionStorage.getItem("expanded-directories")!)` matches the prior entries intact. (confidence: 82)

---

### Revert Test Analysis for Central Behaviors

1. **Strict Compression Verification:**
   * *Behavior:* Storage records are compressed using `serializeStorageValue`.
   * *Revert outcome:* Mentally reverting to uncompressed `JSON.stringify` leaves raw JSON in `sessionStorage`. The test assertion `expect(deserializeStorageValue(sessionStorage.getItem("expanded-directories")!)).toEqual(...)` evaluates to `null` and fails red.
2. **Snapshot Trust Normalization Anchor:**
   * *Behavior:* An oversized valid versioned record (> 1,000 IDs) matching the active workspace retains bounded IDs and remains trusted in `collectOriginalPathOwners` without failing raw-equality.
   * *Revert outcome:* Mentally reverting the snapshot reader to use `collectOne` causes `!equal(read.value, parsed.data)` to evaluate to `true` (since `parsed.data` is truncated to 1,000 IDs). The assertion `expect(owners.trustedFamilies).toContain("expandedDirectories")` fails red.
3. **Workspace Boundary Isolation across Functional Updates:**
   * *Behavior:* Switching `fileWorkspaceAtom` from `ws1` to `ws2` in a real Jotai store prevents `setExpanded((old) => [...old, "dirB"])` from receiving `dirA` from `ws1`.
   * *Revert outcome:* Mentally reverting to an un-scoped atom causes `old` to contain `["dirA"]`. The assertion `expect(store.get(expandedDirectoriesAtom)).toEqual(["dirB"])` fails red.
4. **Untouched Legacy Bytes on Absent Workspace:**
   * *Behavior:* When legacy storage exists and `fileWorkspaceAtom` is `null`, expansions are empty, legacy bytes are untouched, and no repair error is reported.
   * *Revert outcome:* Mentally reverting to eager migration or wiping invalidates `sessionStorage.getItem("expanded-directories")` or invokes `reportPreferenceStorageFailure("repair", ...)`. Assertions `expect(sessionStorage.getItem("expanded-directories")).toBe(rawLegacy)` and verifying no error reporter calls fail red.

VERDICT: APPROVED

## Raw cumulative diff review findings and verdicts

### lens-review-code-quality-diff.txt

[blocker] src/state/expandedDirectories.ts:14 — The persisted format version is repeated as bare `1` in the record type, writer, and parser. Nearby versioned stores name their version (`WORKSPACE_VERSION` in `workspace.ts`, `TREE_STORAGE_VERSION` in `tabStorage.ts`); the parser can drift from the version the writer emits. (origin commit: f8524f03; confidence: 91)

[nit] src/state/expandedDirectories.test.ts:179 — The error assertion hardcodes `65536` while this test already imports `MAX_EXPANDED_DIRECTORY_JSON_BYTES`; derive the expected text from that constant to keep the assertion aligned with the configured limit. (origin commit: f8524f03; confidence: 92)

VERDICT: REVISE

### lens-review-correctness-diff.txt

[blocker] src/state/expandedDirectories.ts:224 — With workspace B active and a valid versioned record for workspace A containing 1,001 IDs, hydration returns no active IDs but exits before repairing the oversized record. Storage therefore remains over the 1,000-ID limit, contrary to the plan’s requirement to repair valid oversized records on hydration (origin commit: `f8524f03`; confidence: 98)

VERDICT: REVISE

### lens-review-error-handling-diff.txt

[should-fix] src/state/expandedDirectories.ts:267 — After an oversized ID is rejected, it remains in memory. A later directory expansion includes it in the updated array, so the oversized-ID check rejects the old ID again, preventing valid additions from being persisted and showing another save error until that directory is collapsed. Origin: `f8524f03` (confidence: 85).

VERDICT: APPROVED

### lens-review-minimalism-diff.txt

[should-fix] src/state/pathOwners.ts:101 — This re-derives the workspace key as `value.id.id`, while the atom uses the existing `fileWorkspaceKey` helper. Use that helper here so both paths share the same identity projection (origin: `f8524f03`; confidence: 95).

[should-fix] src/state/expandedDirectories.ts:275 — The oversized-record `RangeError` message is duplicated at lines 181–183. Extract one shared error builder for both paths (origin: `f8524f03`; confidence: 99).

[nit] src/state/expandedDirectories.test.ts:233 — The storage write-failure spy repeats the setup at lines 205–215, differing only in the error text. Factor a test helper that accepts the message (origin: `f8524f03`; confidence: 92).

[nit] src/state/expandedDirectories.ts:13 — `ExpandedDirectoriesRecord`, `normalizeExpandedDirectoryIds`, and `expandedDirectoryRecordJsonBytes` are exported but have no callers outside this module. Keep these implementation details private (origin: `f8524f03`; confidence: 92).

VERDICT: REVISE

### lens-review-persisted-state-diff.txt

[blocker] src/state/expandedDirectories.ts:219 — If a user upgrades with legacy expansion IDs from workspace A while workspace B is active, this migration tags those unscoped IDs as B. The first startup snapshot distrusts the legacy array, but after reload `src/state/pathOwners.ts:170` trusts the migrated B record and retains A’s capability IDs while B remains active, converting unknown ownership into trusted ownership (origin: decision commit `9c055bd3` / d-20260924-06; implementation commit `f8524f03`; confidence: 92)

VERDICT: REVISE

### lens-review-root-cause-diff.txt

[blocker] src/state/expandedDirectories.ts:216 — The migration assigns every ownerless legacy string to whichever workspace is active. Before `3afed031`, the array held `node.path`; afterward, it held opaque handle IDs but remained global, so it can contain IDs from multiple workspaces. The first startup leaves legacy data untrusted, but this migration labels it with the active workspace ID; on the next startup, `pathOwners.ts:170-179` trusts the record and can retain old-workspace capability IDs still present in the native registry. The new legacy test expects that relabeling and does not cover mixed-workspace data. Origin: `f8524f03`; history shows no earlier fix of this scope failure. (confidence: 97)

Scope note: HEAD remains `f8524f03`, but four reviewed files now have unstaged edits; they are excluded from this verdict.

VERDICT: REVISE

### lens-review-tests-diff.txt

[blocker] src/state/pathOwners.test.ts:263 — The new snapshot cases cover a readable or absent workspace, but not malformed or throwing `file-workspace` storage. Removing the `fileWorkspaceReadable` guard would still pass them while marking expansion owners trusted without a usable workspace identity (origin commit `f8524f03`; confidence: 94)

[should-fix] src/state/expandedDirectories.test.ts:183 — This test supplies invalid JSON, which takes the unreadable-storage path. It does not cover valid JSON with an invalid record shape or version; removing the separate guard that preserves those bytes would overwrite them while the test still passes (origin commit `f8524f03`; confidence: 91)

[should-fix] src/state/expandedDirectories.test.ts:183 — The corrupt-bytes case has a successful `getItem`; no atom test makes reading the key throw. Removing the storage-read catch would let that exception escape without any test going red, instead of reporting the failure and retaining in-memory behavior (origin commit `f8524f03`; confidence: 88)

VERDICT: REVISE
