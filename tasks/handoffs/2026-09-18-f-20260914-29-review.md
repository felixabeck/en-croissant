# Plan-review record: f-20260914-29 Windows PGN cache revision

Load this file before reviewing a successor of this slice. Frozen plan:
`tasks/plans/2026-09-18-windows-pgn-cache-revision.md` (gitignored). Snapshots:
`/tmp/build-f-20260914-29/plan-r1.md` … `plan-r4.md`.

**Mandate:** Close `f-20260914-29`. On Windows, `PgnSnapshotRevision.ctime_nanos` is
`FILE_BASIC_INFO.ChangeTime` from inline `GetFileInformationByHandleEx` on the already-open
PGN handle, so a same-length in-place rewrite that restores LastWriteTime cannot reuse stale
offset-cache ranges. Unix inode `ctime` stays.

**Slice:** `f-20260914-29` only. Left open: `f-20260917-04` (`build`), `f-20260917-13`
(`build`, blocked on `f-20260905-10` by `d-20260918-03`), `f-20260916-10` (`lens`, field rename).

**Decision:** `d-20260918-14`.

## Issues

| ID | Origin | Disposition | Closure |
| --- | --- | --- | --- |
| R1-01 | review-root-cause, review-correctness | Fix | Restore LastWriteTime only; never write ChangeTime. Closed r2. |
| R1-02 | review-root-cause | Skip | No wait on a GitHub log as a phase gate; Windows test id named. |
| R1-03 | review-tests | Fix | Unix test asserts size and mtime equality. Closed r2. |
| R1-04 | review-tests | Fix | Source pin for no `creation_time` fallback. Closed r2. |
| R2-01 | review-plan, review-tests | Fix | Pin proves no-fallback; Io mapping named limitation. Closed r3. |
| R2-02 | review-plan | Fix | Pin scoped to `pgn_snapshot_file`. Closed r3. |
| R2-03 | review-root-cause | Fix | Named residual: ChangeTime is user-settable. Closed r3. |
| R2-04 | review-tests | Fix | Identity equality in the rewrite test. Closed r3. |
| R2-05 | review-tests | Skip | Same as R1-02; CI is post-push. |
| R2-06 | review-root-cause nit | Fix | Mandate wording LastWriteTime only. Closed r3. |
| R3-01 | review-plan | Fix | Query stays inline so the pin slice contains the tokens. Closed r4. |
| R3-02 | review-tests | Skip | Last-write substitution is AC7, not the pin. |
| R4-01 | review-plan | Fix | AC3 wording aligned with Tests 3; no r5 (stale criterion text). |
| D1 | review-minimalism, review-code-quality | Fix | Pin uses `body_at_indent`. `698b7e8c`. |
| D2 | review-code-quality nit | Skip | MaybeUninit vs `zeroed()` sibling idiom. |
| D3 | review-root-cause | Skip | Local Windows runtime; same as R1-02. |
| D4 | review-root-cause | Skip | Named residual already in the plan. |

`plan_adopted_per_round`: r1=3 r2=4 r3=1 r4=1. Four plan-review rounds, all `VERDICT: APPROVED`.
Nine round-1 lenses; four r2; three r3; two r4. Eight cumulative-diff lenses: seven
`APPROVED`, one `REVISE` (minimalism D1, fixed).

Raw reports: `/tmp/build-f-20260914-29/lens-*.txt`, `lens-r2-*.txt`, `lens-r3-*.txt`,
`lens-r4-*.txt`, `lens-diff-*.txt`.

## Code

- `34965285` — `d-20260918-14`.
- `2a23c0b6` — ChangeTime producer and rewrite/pin tests.
- `698b7e8c` — pin through `body_at_indent`.

Executor: DeepSeek Flash via Claude CLI (`routed=claude`). Orchestrator: Grok. Family
separation: the same DeepSeek family wrote the code and ran the lenses; disclosed.
The orchestrator (Grok) arbitrated triage.
