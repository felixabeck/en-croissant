# Plan-review handoff: f-20260905-09 native save-dialog export

Load this file before reviewing a successor of `f-20260905-09`. Inherited issue
IDs: I1–I10. Plan: `tasks/plans/2026-09-19-native-dialog-export.md` (gitignored).
Drain session `d2bacd57-101c-4c1b-982f-5055c39dc8b2`. Decision `d-20260919-06`.

## Mandate (fixed)

Close `f-20260905-09`. The native save-dialog export wrote by pathname
(`save_native_export_blocking` → `crate::infra::fs::atomic_replace(&path)`), the
last counted filesystem-surface reach in `main.rs`. `PathRef` cannot represent
that destination (`d-20260901-03`). `AuthorizedDir` producers cannot express an
arbitrary path (`d-20260905-07`). Specta signatures of `save_board_snapshot` and
`save_engine_logs` stay. Out of mandate: `create_pgn_export_destination` /
`f-20260914-24`; remaining `fs.rs` dest-parent mkdir (`f-20260919-03`); opening
`AuthorizedDir`; changing `PATHNAME_FNS`; renderer/UI.

## Rounds

| Round | Verdicts | Adopted |
|---|---|---|
| r1 | plan/tests/code-quality REVISE; others APPROVED | I1–I8 Fix |
| r2 | plan/tests/correctness/code-quality/tauri-security APPROVED | I9–I10 Fix (reference/comment; no r3 lens) |

`plan_adopted_per_round` r1=8 r2=2.

Raw reports: `/tmp/build-d2bacd57-101c-4c1b-982f-5055c39dc8b2/lens-*.txt`,
`lens-r2-*.txt`, `lens-diff-*.txt`. Delta: `tasks/plans/.plan-delta-r2.diff`.
Snapshots: `/tmp/build-d2bacd57-101c-4c1b-982f-5055c39dc8b2/plan-r1.md`,
`plan-r2.md`.

## Issues

| ID | Claim | Disposition | Closure |
|---|---|---|---|
| I1 | `cargo test NativeExportDest` matches 0 tests, exits 0 | Fix | snake_case `native_export` filter; measured unmatched filter exit 0 |
| I2 | No test red on pathname revert | Fix | unix parent-rename test on `NativeExportDest` |
| I3 | Pin caller to dest type, not checker count alone | Fix | source-shape pin in `main.rs` |
| I4 | Wrong-extension must prove order | Fix | missing parent + `InvalidInput` |
| I5 | `atomic_replace_at` doc said authority-only | Fix | comment rewritten |
| I6 | Symlink test must go through dest type | Fix | `from_save_path` then `replace`, `#[cfg(unix)]` |
| I7 | Do not edit engine-image pin `:4670` | Fix | left alone |
| I8 | Call takes `&self.parent, &self.leaf` | Fix | references |
| I9 | Rest of doc still said capability resolution | Fix | folded into I5 |
| I10 | `replace` pin must name `include_str!("infra/fs.rs")` | Fix | pin reads both files |

## What shipped

- `NativeExportDest` in `infra/fs.rs`: `from_save_path` then `atomic_replace_at`.
- `save_native_export_blocking` uses it; dialog still precedes accepted ownership.
- `main.rs` removed from `FS_SURFACE_ALLOWLIST` and `INITIAL_FS_SURFACE_COUNTS`.
- Implementation commit `f26a6260`. Decision `d-20260919-06`.

## Cumulative review

Executor Claude wrote the code; Grok orchestrated. Detection on Claude (same
family as the writer) — disclosed. Cumulative lenses over
`8658a105..HEAD`: correctness, root-cause, tauri-security, tests, code-quality,
error-handling, minimalism, ipc-contract all APPROVED. code-quality should-fix
that the finding was still `Status: open` is closed by the handled header.
tests nits (source-shape vs behaviour; unix-only dest tests) Skip.

## Successor

`f-20260914-24` (PGN export cleanup discard) and `f-20260919-03` (remaining
`fs.rs` dest-parent mkdir / dest-file `atomic_replace`) stay open at their
filed tiers. Load this file before review. Do not re-open `d-20260919-06`
without new evidence.
