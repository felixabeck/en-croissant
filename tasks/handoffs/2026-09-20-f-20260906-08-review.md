# Plan-review record — f-20260906-08, engine-install executable bit

**Run:** 2026-09-20, `build` via `next-finding`, orchestrator Claude Code (Opus 5), executor
Codex (`gpt-5.6-luna`). Full auto.
**Mandate:** finding `f-20260906-08` in `tasks/findings.md`, verbatim, fixed across every round.
**Plan (ignored by git, so it may not survive):** `tasks/plans/2026-09-20-engine-install-executable-bit.md`.
**Raw lens reports (ephemeral):** `/tmp/build-cf-f20260906-08/` — `lens-*.txt` (round 1),
`r2lens-*` … `r10lens-*` (rounds 2-10), `dlens-*.txt` (cumulative diff review).
**Implementation commits:** `ac9fd92a` (the invariant and its chain test), `df4d70bd` (retiring
the dead command). **Decisions:** `d-20260920-06`, superseding `d-20260906-06`.

This file is the durable record. The plan file and the raw reports above are not tracked and can
disappear; everything needed to recover the issue history is below. A successor that inherits any
issue from this run must load this file before its own review.

**Inherited by:** nothing. Every issue opened in this run carries an explicit disposition and
every adopted correction carries a recorded closure check; no issue was deferred to a successor.
One finding raised during review was **rejected as already filed** — `f-20260920-01`
(`atomic_install_download_dir` reopening its staging and target directories by pathname), which
owns that defect and is unrelated to this mandate.

**Outcome:** 10 plan-review rounds, then 9 cumulative-diff lenses, all APPROVED, with one
`should-fix` repaired (an internal inconsistency in the `d-20260920-06` record, which required
re-recording the decision because the ledger's mutation receipt refuses a hand edit).

---

## Reviews

### Round 1 — 2026-09-20, ten lenses, plan revision r1

Raw reports: `/tmp/build-cf-f20260906-08/lens-*.txt`. Raw verdicts as returned:
`review-plan` REVISE · `review-tests` REVISE · `review-tauri-security` REVISE ·
`review-minimalism` APPROVED · `review-correctness` APPROVED · `review-root-cause` APPROVED ·
`review-error-handling` APPROVED · `review-engine-protocol` APPROVED ·
`review-ipc-contract` APPROVED · `review-code-quality` APPROVED.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R1-01 | `## Reviews` carries no revision identity / history / open issue IDs | review-plan (100) | **Skip** — false positive: this *is* round 1, and by `review-lens-contract.md` a round-1 prompt carries no history block. The section is now populated for round 2. |
| R1-02 | Tests inspect mode only; none executes the installed binary, so launchability is inferred, not observed | review-plan (96), review-tests (92), review-error-handling (94) | **Fix** — Obligation C step 5 now execs the installed file and asserts its output. MANDATE obligation: "the default-engine install cannot launch what it installs". |
| R1-03 | No test forces `mark_engine_executable` to fail; a discarded `Result` or a persist-before-chmod would stay green | review-plan (95), review-tests (99), review-error-handling (95) | **Fix, partly argued** — new Obligation D. The runtime failure is named as unstageable without root or a second uid; the `?` and the ordering are pinned by the retargeted source scan, itself staged assertion by assertion. |
| R1-04 | Phase 2 proof omits `pnpm build-vite` and never actually runs `bindings:generate`; the exporter needs `../dist` | review-plan (97) | **Fix** — verified against `.github/workflows/test.yml:159-167` and `scripts/check-bindings.mjs:13-39`. Phase 2's proof is now four ordered commands. |
| R1-05 | The retargeted test keeps the `set_file_as_executable_blocking` identifier, contradicting acceptance criterion 2 | review-plan (92) | **Fix** — the test is renamed `register_installed_engine_marks_executable_without_off_unix_refusal`. |
| R1-06 | Removing the source-scan tuples does not assert the deleted symbols are absent; a dead helper could survive green | review-tests (96) | **Fix** — fourth assertion added to the retargeted scan. |
| R1-07 | The stated reason for excluding `verify:app` is false: the default-engine card calls `installDefaultEngine` directly, not the picker | review-plan (97) | **Fix** — verified at `src/components/engines/AddEngine.tsx:222-228`. The exclusion stands; its reason is corrected to the network-and-signature dependency. |
| R1-08 | Proof commands omit the backend coverage ratchet and the cross-layer gates | review-tests (91) | **Fix** — the step-8 affected gate set is now named in this plan. |
| R1-09 | Two tests duplicate one registration invariant | review-minimalism (93) | **Fix** — the separate unit test is dropped; one chain test remains, and it strictly covers the other. |
| R1-10 | `atomic_install_download_dir` drops the authority-retained parent descriptor and reopens `target` by pathname (TOCTOU outside the authorised root) | review-tauri-security (97) | **Skip (already filed)** — this is `f-20260920-01`, open, `entry=lens`, with the same mechanism and the `acquire_target` + stored-path-comparison fix shape. Outside MANDATE; not folded in (`push-review-policy` §4, separate-design-question `Defer`). |
| R1-11 | A chmod failure reaches the user as a generic "I/O failure" | review-error-handling (91) | **Skip** — settled by design. `Error::Io` deliberately carries no raw backend diagnostic to the renderer (`.claude/rules/async-resource-invariants.md`, "never move a bearer token or raw backend diagnostic into the renderer"), while the typed `ErrorCategory::Permission` *is* preserved (`src-tauri/src/error.rs:257-261`). Not a defect this plan introduces. |
| R1-12 | The plan cites "Rule 4" for the optimal-long-term gate; the supplied contract assigns that to Rule 2 | review-code-quality (95) | **Skip** — false positive. The lens was given the project `CLAUDE.md`; the optimal-long-term gate is rule 4 of the user-wide `~/.claude/CLAUDE.md`, which is not a lens input. |

Adopted this round (triage `Fix`): 8 — R1-02, R1-03, R1-04, R1-05, R1-06, R1-07, R1-08, R1-09.
Opened: R1-01 … R1-12. Resolved by the r2 revision: R1-02 … R1-09. Skipped with reason:
R1-01, R1-10, R1-11, R1-12. Open after round 1: none pending disposition.
Elapsed: round 1 ran ~13 min wall across ten parallel leaves; no non-overlapping waits.

### Round 2 — 2026-09-20, seven lenses, plan revision r2

Raw reports: `/tmp/build-cf-f20260906-08/r2lens-*.txt`. Delta:
`tasks/plans/.plan-delta-r2.diff`. Raw verdicts as returned:
`review-plan` REVISE · `review-tauri-security` REVISE · `review-tests` APPROVED ·
`review-error-handling` APPROVED · `review-minimalism` APPROVED · `review-correctness` APPROVED ·
`review-engine-protocol` APPROVED.

**Round-1 correction closure against r2.** R1-04, R1-05, R1-06, R1-07, R1-08, R1-09 were reported
closed by every lens that carried them. R1-02 and R1-03 were closed by six lenses and reported
**not closed** by `review-plan`, on the ground that the *mechanism* was right but its staged-failure
evidence was impossible to produce as specified. That is the arbiter's reading too, so R1-02 and
R1-03 stay open into round 3 and are closed by the r3 corrections below rather than by assertion.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R2-01 | The mode assertion runs before the exec assertion, so removing the chmod fails fast and the exec assertion's own `EACCES` message can never be observed — R1-02/R1-03's evidence requirement is impossible as written | review-plan (98), review-tests (96) | **Fix** — Obligation C step 3 now measures all three observations first and asserts afterwards, each with a message unique to that assertion, so one staged break yields every affected assertion's own message. |
| R2-02 | The `0o600` premise cannot be staged by changing the archive fixture: `write_zip` writes bytes with default options and extraction hardcodes `0o600`, ignoring archive modes | review-plan (99), review-tauri-security (98), review-error-handling (97) | **Fix** — verified at `src-tauri/src/infra/fs.rs:4428-4438` and `src-tauri/src/fs.rs:3640`. The staged break is now the extraction mode constant itself, altered in the working tree and restored — production input to the test, never the test's own logic (`push-review-policy` §2). |
| R2-03 | Deleting the command reverses `d-20260906-06`, which is still `Superseded-by: -`; a reversal path written in a run plan is not the record | review-plan (98) | **Fix** — verified at `tasks/decisions.md` `d-20260906-06`. It is not a reversal: that decision kept the command *for* the wiring run, which this is. But the state it created ends here, so phase 2 records a successor through `record-decision` and points its `Superseded-by` with `set-trailer`. New sub-section under *Decisions*. |
| R2-04 | `engine_destination` returns `(PathAuthority, PathRef, PathBuf)`; `register_installed_engine` needs `&EngineRootHandle`, which the helper discards | review-plan (99) | **Fix** — verified at `src-tauri/src/fs.rs:2037-2051` and `path_authority/mod.rs:5593-5597`. The helper returns the handle as a fourth element and its four call sites are updated; reconstructing it from the destination `PathRef` is explicitly rejected as coupling to a coincidence. |
| R2-05 | Phases 1 and 2 both modify `src-tauri/src/fs.rs`, contradicting the file-disjoint decomposition | review-plan (97) | **Fix (wording)** — the plan never claimed disjointness, but it did not say the opposite either. Phase 2 now states plainly that the phases share a file, are strictly sequential, and are split by decision rather than by file, under rule 4a. |

Adopted this round (triage `Fix`): 5 — R2-01 … R2-05. Opened: R2-01 … R2-05. Carried open from
round 1: R1-02, R1-03 (mechanism accepted, evidence not yet producible). No finding was rejected
this round. `review-tauri-security` re-confirmed that `f-20260920-01` stays separate and did not
re-report it.
Elapsed: round 2 ran ~17 min wall across seven parallel leaves; no non-overlapping waits.
Cumulative completed rounds: 2.

<!-- appended per round; raw lens output preserved -->

### Round 3 — 2026-09-20, five lenses, plan revision r3

Raw reports: `/tmp/build-cf-f20260906-08/r3lens-*.txt`. Delta:
`tasks/plans/.plan-delta-r3.diff`. Raw verdicts as returned:
`review-plan` REVISE · `review-tests` REVISE · `review-correctness` APPROVED ·
`review-error-handling` APPROVED · `review-tauri-security` APPROVED.

**Carried-issue closure against r3.** R2-02, R2-04 and R2-05 were reported closed by every lens
that carried them. R1-02, R1-03, R2-01 and R2-03 split: `review-error-handling` and
`review-tauri-security` closed all four, `review-plan` closed none of them, `review-tests` closed
R1-02, R2-01 and R2-03 but not R1-03. In every case the dissent was specific and checkable rather
than a difference of taste, and in every case it was **right on the facts** — see R3-01 through
R3-04 below. The arbiter therefore keeps R1-02, R1-03, R2-01 and R2-03 open into round 4.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R3-01 | Measuring before asserting does not defeat fail-fast `assert!`: three asserts in a row still stop at the first, so the exec check's own message is still unobservable | review-plan (100) | **Fix** — correct, and it invalidates r2's answer to R2-01. The test now pushes a unique message per failed check onto a `Vec<String>` and ends with one `assert!(failures.is_empty(), …)`. |
| R3-02 | The source-scan matrix cannot use a "scratch copy": `source_for` resolves every file through compile-time `include_str!` with no runtime hook | review-plan (100) | **Fix** — verified at `platform_support.rs:1803-1818`. Staging now edits the real source, smallest change, restored immediately, restoration proven green — which `push-review-policy` §2 permits and requires to be recorded. The verifier itself is still never edited. |
| R3-03 | A staged-failure record kept only in a phase report violates the artifact-attached rule; no persistent location was named | review-plan (95) | **Fix** — verified against `push-review-policy` §2 ("The record lives with the artefact, in its module README or its own header, not in a chat message"). The matrix goes into doc comments above both tests, following `scripts/check-bindings.mjs:1-4`. |
| R3-04 | Phase 2 mutates `tasks/decisions.md`, which is absent from its file list, and `record-decision` needs a pending entry file | review-plan (98) | **Fix** — added to phase 2's files, with both ledger commands and the pending-entry input named. |
| R3-05 | `engine_destination` has three callers, not four | review-plan (100) | **Fix** — verified: `fs.rs:3838`, `:3997`, `:4043`. Corrected; the new test is the fourth. |
| R3-06 | "no spelling survives anywhere" is impossible and exceeds the criterion — the ledgers and this plan necessarily keep naming the command | review-plan (99) | **Fix** — the claim is scoped to `src-tauri/src/fs.rs` and `src-tauri/src/main.rs`, and the records are named as deliberately untouched (rule 4c). |
| R3-07 | The final-gate list omits `git diff --check` | review-plan (97) | **Fix** — verified at `.claude/skills/push/SKILL.md` §4; added. |
| R3-08 | The four source-scan assertions share the module's bare `"{body}"` diagnostic, so a red run cannot say which failed | review-tests (96) | **Fix** — each assertion now carries a message unique to it, which `push-review-policy` §2 requires of anything a matrix distinguishes. |
| R3-09 | Breaking `create_regular_at`'s `0o600` also reddens `owned_staging_dir_*`'s own `0o600` assertion, so "nothing else does" is false | review-correctness (99) | **Fix** — verified at `src-tauri/src/infra/fs.rs:9385`. The collateral failure is named in the matrix and the staged run is filtered to the new test for message capture. |

Adopted this round (triage `Fix`): 9 — R3-01 … R3-09. Opened: R3-01 … R3-09. Carried open from
earlier rounds: R1-02, R1-03, R2-01, R2-03. No finding was rejected this round.
Elapsed: round 3 ran ~20 min wall across five parallel leaves; no non-overlapping waits.
Cumulative completed rounds: 3. Note: every round-3 correction is verification machinery, not a
change to the intended behaviour, ownership or contract — Obligations A and B have been textually
settled since r1.

### Round 4 — 2026-09-20, three lenses, plan revision r4

Raw reports: `/tmp/build-cf-f20260906-08/r4lens-*.txt`. Delta:
`tasks/plans/.plan-delta-r4.diff`. Raw verdicts as returned:
`review-plan` REVISE · `review-tests` REVISE · `review-correctness` REVISE.

**Carried-issue closure against r4.** All three lenses independently closed R1-02, R2-01, R2-03,
R3-01 and R3-03 through R3-09, and all three left R1-03 and R3-02 open for the **same single
reason**: the corrected staging route was written into Obligation D but the superseded sentence
was left standing in Phase 2's proof, so the plan contradicted itself. That is one editing miss,
not a disagreement about the mechanism.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R4-01 | Phase 2 still instructs staging the source-scan assertions "against a scratch copy", contradicting Obligation D's corrected real-source route and leaving R1-03 / R3-02 unprovable | review-plan (100), review-tests (99), review-correctness (100) | **Fix** — the stale sentence is replaced; Phase 2 now restates the real-source route, the reason (`source_for` is compile-time `include_str!`), the restoration proof, and where the matrix lives. |
| R4-02 | `fs.rs:3640` is cited as `write_zip`; that line is `zip_payload`, and `write_zip` is at `fs.rs:2554` | review-plan (100), review-correctness (100) | **Fix** — verified; both citations corrected. The stale reference inside the round-2 table is left as written, because `## Reviews` is append-only and records what was verified at the time. |

Adopted this round (triage `Fix`): 2 — R4-01, R4-02. Opened: R4-01, R4-02. Carried open from
earlier rounds and still open into round 5: R1-03, R3-02 — both on R4-01's single cause.
No finding was rejected this round.
Elapsed: round 4 ran ~12 min wall across three parallel leaves; no non-overlapping waits.
Cumulative completed rounds: 4.

### Round 5 — 2026-09-20, three lenses, plan revision r5

Raw reports: `/tmp/build-cf-f20260906-08/r5lens-*.txt`. Delta:
`tasks/plans/.plan-delta-r5.diff`. Raw verdicts as returned:
`review-plan` REVISE · `review-tests` APPROVED · `review-correctness` APPROVED.

**Carried-issue closure against r5.** All three lenses closed R1-03, R3-02, R4-01 and R4-02. No
issue opened before round 5 remains open.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R5-01 | The chain test's exact-mode assertions do not neutralise the process umask; a created file's mode is the requested mode intersected with it, so the test is accidental under `umask 077` and red under a hostile one — and the staged `0o600` → `0o700` break proves nothing | review-correctness (98) | **Fix** — verified at `src-tauri/src/fs.rs:1428` and the existing precedent `archive_directory_creation_sites_are_private_with_zero_umask` (`fs.rs:3206-3208`). The test now holds `UMASK_TEST_LOCK` and `UmaskGuard::zero()`. |
| R5-02 | Phase 2 mutates `tasks/decisions.md`, but the supplied `FILES` manifest omits it | review-plan (98) | **Skip (defect in the review packet, not the plan)** — Phase 2's own file list has named `tasks/decisions.md`, with both ledger commands, since r4 (the R3-04 correction). What omitted it was the `FILES:` line of the lens input, which the arbiter writes. Corrected for round 6; no plan change is warranted. |
| R5-03 | Obligation A requires marking executable before `resolved.identity()`, which is beyond the mandate and beyond what the source scan pins | review-plan (92) | **Fix** — correct, and it is the kind of over-specification plan review exists to catch. The identity is a `(dev, ino)` pair that `fchmod` does not change, so the requirement is reworded to mark-before-persist only, with the reason stated. |

Adopted this round (triage `Fix`): 2 — R5-01, R5-03. Opened: R5-01, R5-02, R5-03. Rejected:
R5-02, with its reason. Carried open into round 6: none from rounds 1-4.
Elapsed: round 5 ran ~11 min wall across three parallel leaves; no non-overlapping waits.
Cumulative completed rounds: 5.

### Round 6 — 2026-09-20, two lenses, plan revision r6

Raw reports: `/tmp/build-cf-f20260906-08/r6lens-*.txt`. Delta:
`tasks/plans/.plan-delta-r6.diff`. Raw verdicts as returned:
`review-plan` REVISE · `review-correctness` APPROVED.

**Carried-issue closure against r6.** Both lenses closed R5-01 and R5-03. Nothing from rounds 1-5
remains open.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R6-01 | No phase touches `tasks/findings.md`, so `f-20260906-08` would stay `Status: open` after the work is done | review-plan (99) | **Fix** — correct, and it is the failure mode that makes a drain loop on the same finding forever. New phase 3 runs `annotate`, `set-header --status handled` and `check` through the locked commands, after phase 2 is green. |
| R6-02 | The `kit sync --check` final gate is already red because the vendored `scripts/findings.py` is behind agent-kit, and no phase repairs it | review-plan (95) | **Skip (stale evidence, measured)** — the lens read `f-20260906-07`'s 2026-09-20 push-blocked note. Both blockers in that note have since been cleared on `master`: `8c5a7134 chore(findings): vendor agent-kit 4dbc041` and `4d025de9 perf(bundle): load the flag pack on demand…`, and `HEAD` equals `@{u}`. Measured at plan time: `env -u KIT_ROOT pnpm findings:kit:check` → `ok .` (exit 0). Re-checked at step 8 like every other gate. |
| R6-03 | The staged-break citation says `infra/fs.rs:4437`; `Mode::from_raw_mode(0o600)` is at `:4436` | review-plan (99) | **Fix** — verified; corrected. |

Adopted this round (triage `Fix`): 2 — R6-01, R6-03. Opened: R6-01, R6-02, R6-03. Rejected:
R6-02, with the measurement that refutes it. Carried open into round 7: none.
Elapsed: round 6 ran ~8 min wall across two parallel leaves; no non-overlapping waits.
Cumulative completed rounds: 6.

### Round 7 — 2026-09-20, one lens, plan revision r7

Raw report: `/tmp/build-cf-f20260906-08/r7lens-plan.txt`. Delta:
`tasks/plans/.plan-delta-r7.diff`. Raw verdict as returned: `review-plan` REVISE.
Only `review-plan` ran: both round-6 corrections were its own findings, and no other lens's
coverage was newly affected.

**Carried-issue closure against r7.** R6-01 and R6-03 closed. Nothing from rounds 1-6 remains open.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R7-01 | The plan wrongly excludes `pnpm verify:app`: this diff changes the real IPC registry, and the project rule is that such a diff is observed in the actual product; `verify:app` reaches real WebKitGTK and real IPC without needing an engine download | review-plan (98) | **Fix** — correct, and the earlier reasoning confused two questions. *Can `verify:app` exercise the install flow?* No, and that is unchanged. *Should it run at all?* Yes: `CLAUDE.md` says to run it by hand when a diff changes lifecycle, IPC or process teardown, and deleting a registered command with a regenerated binding is that. New Phase 4 runs `pnpm build` and `pnpm verify:app`, with what it does and does not prove stated, and a missing driver named as an unmet precondition rather than a pass. |
| R7-02 | The successor supersedes `d-20260906-06` wholesale while declaring its `cancel_download` half out of scope; `set-trailer` supersedes the entry as a whole, so the ledger would be left half-answered | review-plan (92) | **Fix** — the successor now disposes of both branches explicitly, including `f-20260906-07`'s already-handled cancellation evidence, and says the three superseded commands stay with `f-20260906-11`. |

Adopted this round (triage `Fix`): 2 — R7-01, R7-02. Opened: R7-01, R7-02. No finding was
rejected this round. Carried open into round 8: none.
Elapsed: round 7 ran ~6 min on one leaf; no non-overlapping waits.
Cumulative completed rounds: 7. Note: R7-01 adds a verification phase, not behaviour; Obligations
A and B remain textually settled since r1, and Obligation C since r6.

### Round 8 — 2026-09-20, one lens, plan revision r8

Raw report: `/tmp/build-cf-f20260906-08/r8lens-plan.txt`. Delta:
`tasks/plans/.plan-delta-r8.diff`. Raw verdict as returned: `review-plan` REVISE.
Only `review-plan` ran: both round-7 corrections were its own findings, and no other lens's
coverage was newly affected.

**Carried-issue closure against r8.** R7-01 and R7-02 closed. Nothing from rounds 1-7 remains open.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R8-01 | Phase 4's precondition list names `webkit2gtk-driver` and `tauri-driver` but omits `kwin_wayland`, which the driver also starts | review-plan (99) | **Fix** — verified at `scripts/app-driver.mjs:139-148`. All three binaries are now named, and the not-run report must say which one was absent. |

Adopted this round (triage `Fix`): 1 — R8-01. Opened: R8-01. No finding was rejected this round.
Carried open into round 9: none.
Elapsed: round 8 ran ~5 min on one leaf; no non-overlapping waits.
Cumulative completed rounds: 8.

### Round 9 — 2026-09-20, one lens, plan revision r9

Raw report: `/tmp/build-cf-f20260906-08/r9lens-plan.txt`. Delta:
`tasks/plans/.plan-delta-r9.diff`. Raw verdict as returned: `review-plan` REVISE.

**Carried-issue closure against r9.** R8-01 closed. Nothing from rounds 1-8 remains open.

| ID | Claim | Witnesses | Disposition |
|---|---|---|---|
| R9-01 | The successor decision would cite `src/utils/downloadJobs.ts`, which does not exist; the wired cancellation is at `src/hooks/downloadJobs.ts:102` | review-plan (100) | **Fix** — verified: `src/utils/downloadJobs.ts` does not exist, `src/hooks/downloadJobs.ts` does. Corrected. The path would have gone into `tasks/decisions.md`, where a wrong one survives every future reader (rule 4c). |

Adopted this round (triage `Fix`): 1 — R9-01. Opened: R9-01. No finding was rejected this round.
Carried open into round 10: none.
Elapsed: round 9 ran ~4 min on one leaf; no non-overlapping waits.
Cumulative completed rounds: 9.

### Round 10 — 2026-09-20, one lens, plan revision r10 — CLOSED

Raw report: `/tmp/build-cf-f20260906-08/r10lens-plan.txt`. Delta:
`tasks/plans/.plan-delta-r10.diff`. Raw verdict as returned: `review-plan` APPROVED.

R9-01 closed against r10, with the corrected consumer verified at
`src/hooks/downloadJobs.ts:102-112`. No new findings.

**Closure.** Every issue opened in rounds 1-9 — R1-01..R1-12, R2-01..R2-05, R3-01..R3-09,
R4-01, R4-02, R5-01..R5-03, R6-01..R6-03, R7-01, R7-02, R8-01, R9-01 — carries an explicit
disposition, and every adopted substantive correction carries a recorded reviewer closure check
against the revision that made it. Adopted (`Fix`): 32. Rejected with evidence: 6 — R1-01,
R1-10 (filed as `f-20260920-01`), R1-11, R1-12, R5-02, R6-02. Unique issues opened: 38; withdrawn:
none; open at closure: none. Rewrites or splits: none — the MANDATE was never rewritten and the
plan was never split.

`plan_adopted_per_round`: r1=8 r2=5 r3=9 r4=2 r5=2 r6=2 r7=2 r8=1 r9=1 r10=0.
Cumulative completed rounds: 10. Total review elapsed ≈ 96 min wall, all of it active review
(parallel leaf durations are not summed); no quota, dependency or release waits.

Two things this history is worth reading for. **The defect classes moved.** Rounds 1-2 found
mandate-level gaps — launchability inferred rather than observed, an unproven failure path, a
missing decision record. Rounds 3-5 found that the *evidence* for those corrections was
unproducible as specified: fail-fast assertions, a source scanner with no runtime hook, an
umask nobody had neutralised. Rounds 6-9 found citations and preconditions. Nothing in rounds 3-10
changed intended behaviour, ownership or a contract; Obligations A and B have been textually
settled since r1. **And the majority was wrong twice.** In rounds 3 and 4 the arbiter kept issues
open against a 2-to-1 and a 6-to-1 majority of lens closures, on `review-plan`'s specific,
checkable dissent — and in both cases the dissent was right on the facts.
