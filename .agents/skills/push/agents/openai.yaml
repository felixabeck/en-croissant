interface:
  display_name: "Push ChessFable"
  short_description: "Validate, review and push ChessFable safely"
  default_prompt: "Use $push to validate, independently review, commit, and push the current ChessFable changes."
